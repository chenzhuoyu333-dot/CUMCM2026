# A 题 COMSOL 操作手册（问题 2 / 3 / 4）

适用：COMSOL 6.3 + 传热模块 + 基础平台数学模块
所有界面名称均已对照本机安装的官方文档核实。
**问题 1 已完成并验证（与独立有限体积程序偏差 < 0.17%），本手册从问题 1 的模型出发。**

---

# 第 0 章 动手前必须先确认的事

## 0.1 问题 1 的模型必须已修正（最重要）

点 **系数型偏微分方程 (c)**，逐项核对：

| 位置 | 必须是 | 如果不是 |
|---|---|---|
| 阻尼或质量系数 `da` | **`r`** | 改成 `r` |
| 扩散系数 `c` | **`r*D_coef`** | 改成 `r*D_coef` |
| 对流系数 β、吸收 a、源项 f、ea、α、γ | 全部 `0` | 清成 0 |
| 「单位」区 → 源项物理量 | 单位 = **`m/s`** | 改成 `m/s` |
| 通量/源 1 → 边界通量 `g` | **`R_drug*h_mass*(C_air-C)`** | 照抄 |

**为什么**：COMSOL 的 PDE 接口在轴对称几何里的 ∇ 是**普通偏导数**，不含柱坐标的曲率项。
官方文档明确要求："**multiply the entire equation, as well as its boundary conditions, by a volume factor — in an axisymmetric geometry, for example, with a factor r**"。
不乘这个 `r`，解出来的是**平板一维**方程，30 分钟的表面含水率会偏高约 3.5%。

## 0.2 全程都要用这两样

| 文件 | 作用 |
|---|---|
| `csv_to_result.py` | 把 COMSOL 导出的 csv 转成模板要求的 xlsx |
| 本文档 | 每问的改动清单 |

## 0.3 四问的取样间隔

COMSOL 导出的是网格节点上的值，节点间距由网格决定：

| 网格最大单元大小 | 单元数 | 节点间距 | **取样间隔** |
|---|---|---|---|
| `2e-4` m | 100 | 0.0002 m | **5** |
| `5e-5` m | 400 | 0.00005 m | **20** |

取样间隔 = **0.001 m ÷ 节点间距**（0.001 m = 0.1 cm）。

## 0.4 关于图线：题目不要求画图

**四问的题目里没有任何一句要求画图。** 要求只有两样：

1. 论文里的表格（表 1 ~ 表 6）
2. 结果文件（result1 ~ result4.xlsx）

图是**论文自己要用的**。评审看的是"结论有没有讲清楚"，不是图多不多。所以：

- 每问画 **2 ~ 3 张关键图**就够了
- 画图卡顿的根源是"每个时间步画一条曲线"（问题 2 有 10801 个时间步！），
  解决办法见**第 5 章**

## 0.5 每问的数据量与图线规划

| | 论文表格要多少个数 | result 文件要多少格 | 建议画几张图 |
|---|---|---|---|
| 问题 1 | 表1、表2：各 7 × 5 = 35 | 1800 × 21 | 2 张 |
| **问题 2** | **表3、表4：各 6 × 5 = 30** | **10801 × 21** | **2 ~ 3 张** |
| 问题 3 | 表5：6 h 间隔 × 5 列 | 数千 × 21 | 2 张 |
| 问题 4 | 表6：6 h 间隔 × 5 列 + 表面列 | 数千 × 21 | 2 ~ 3 张 |

**表格数量和结果文件数量差得很远**：论文表 4 只要 **30 个**水分值，
而 result2.xlsx 有 **22 万个**。填表时从 result2.xlsx 里挑那 30 个即可，
其余数据是给评审复核用的。

---

# 第 1 章 文件管理

**每问都从上一问另存，绝不重新建模，也绝不在原文件上改。**

```
A_问题1.mph   ← 已完成
A_问题2.mph   ← 打开问题1 → 文件 → 另存为
A_问题3.mph   ← 打开问题2 → 文件 → 另存为
A_问题4.mph   ← 打开问题3 → 文件 → 另存为
```

> COMSOL 的 `.mph` 保存时**连网格和解一起存**（文档原文：*Solved MPH-files include all meshes and solutions*），
> 关掉再打开结果还在。每个文件建议命名成 `A_问题2_日期.mph`，方便回溯。

---

# 第 2 章 问题 2：整个烘干过程的前 3 小时

## 2.0 目标

用**附录 3 的经验公式**（物性随 C、T 变化），求出烘干过程前 3 小时内药材的温度和水分浓度分布。

| 交付物 | 内容 |
|---|---|
| 论文表 3 | **6 个时刻 × 5 个位置 = 30 个温度值**（0.5、1.0、1.5、2.0、2.5、3.0 h × 0、0.5、1、1.5、2 cm） |
| 论文表 4 | **同为 30 个水分值** |
| result2.xlsx | 每隔 1 s、每隔 0.1 cm 的**完整结果**（10801 × 21 = 22 万格，两个工作表） |

### 题干原文（不要看漏后半句）

> ……在论文中按表 3 和表 4 的格式分别给出 3 h 内每隔 0.5 h、到药材中心距离 0、0.5、1、1.5、2 cm 处的结果，
> **并将每隔 1 s、到药材中心距离每隔 0.1 cm 的完整结果保存到文件 result2.xlsx 中**（模板文件见附件 3）。

**结论：result2.xlsx 是题干的硬性要求，而且必须是"完整结果"，不能只交 30 个数。**

- 论文表格：**30 个数**（写进正文）
- result2.xlsx：**10801 行 × 21 列 = 22 万格**（作为附件提交）

> 填表时从 result2.xlsx 里挑那 30 个即可；其余数据是给评审复核用的。
>
> **题目没有要求画图。** 图是论文自己配的，每问 2 ~ 3 张就够，看第 5 章。

## 2.1 另存文件

1. 打开 `A_问题1.mph`
2. **文件 → 另存为** → 存成 `A_问题2.mph`
3. 之后所有改动都在新文件里做

## 2.2 改动 1：物性换成附录 3

点 **固体传热 (ht) → 固体 1**，三项都选 **用户定义**，填：

| 设置项 | 表达式 |
|---|---|
| 导热系数 k | `0.21[W/(m*K)]+0.38[W/(m*K)]*C/(C+1)` |
| 密度 ρ | `650[kg/m^3]+128[kg/m^3]*C` |
| 比热容 Cp | `1450[J/(kg*K)]+2736[J/(kg*K)]*C/(C+1)` |

## 2.3 改动 2：变量 D_coef 换成附录 3

点 **组件 1 → 定义 → 变量 1**，把 `D_coef` 的表达式改成：

```
2.4e-3[m^2/s]*exp(-0.45/C)*exp(-3850[K]/T)
```

> 注意指数里的 **T 是开尔文**，所以写 `3850[K]/T`。
> `T_air`、`C_air`、`T_C` 三行不用改。

## 2.4 改动 3：时间范围

1. 展开 **研究 1** → 点 **步骤 1：瞬态**
2. **时间单位**：`s`
3. **输出时步**：

```
range(0,1,10800)
```

（3 小时，每秒一个点）

## 2.5 计算

右键 **研究 1** → **计算**。

> **切记**：改完物性和时间范围**必须重新计算**，否则导出的是旧解。

**检查点**：算完后双击 **温度 (ht)** 绘图组，温度应该在 28 ~ 50 °C 之间。

## 2.6 导出

数据集仍用 **研究 1/解 1**（**不要**用"一维栅格"，它算不了场变量）。

### 导出温度

1. **右键「结果 → 导出」** → **数据**
2. **数据集** = `研究 1/解 1`
3. **表达式** = `T_C`
4. **文件名** = `comsol_p2_T.csv`
5. 点 **导出**

### 导出水分浓度

6. 再 **右键「结果 → 导出」** → 新建第二个 **数据** 节点
7. **数据集** = `研究 1/解 1`，**表达式** = `C`，**文件名** = `comsol_p2_C.csv`
8. 点 **导出**

> 「高级」里的选项保持默认即可：
> 包含标题 ✅ / 全精度 ✅ / 排序 ❌ / 包含坐标 ✅ / 包含 NaN ✅ / 计算位置 = 拉格朗日点 / 分隔符 = 逗号

## 2.7 生成 result2.xlsx

在放 csv 的目录里打开 PowerShell，运行：

```
python "C:\Users\Wil\Documents\ChatGPT\CUMCM\csv_to_result.py" comsol_p2_T.csv comsol_p2_C.csv result2.xlsx 20

```

（末尾的 `20` 是取样间隔，对应 400 个单元的网格；若用 100 个单元就写 `5`）

## 2.8 填论文表 3、表 4

从 result2.xlsx 里取这六行（1 h = 3600 s）：

| 表里的时间 | result2.xlsx 里的时间行 |
|---|---|
| 0.5 h | 1800 |
| 1.0 h | 3600 |
| 1.5 h | 5400 |
| 2.0 h | 7200 |
| 2.5 h | 9000 |
| 3.0 h | 10800 |

距离列取 **0、0.5、1、1.5、2 cm** 五列（对应表格里的第 1、6、11、16、21 列）。

---

# 第 3 章 问题 3：求烘干所需时间

## 3.0 目标

物性与问题 2 **完全相同**。要求出"药材**各处**水分浓度都低于 0.15 kg/kg"所需的时间。

| 交付物 | 内容 |
|---|---|
| 论文表 5 | 每隔 6 h × 每隔 0.5 cm 的水分浓度 |
| 论文正文 | **烘干所需时间（h）** |
| result3.xlsx | 每隔 60 s、每隔 0.1 cm 的完整结果 |

## 3.1 另存文件

打开 `A_问题2.mph` → **文件 → 另存为** → `A_问题3.mph`

**物性一个都不用改。**

## 3.2 改动：时间范围拉长

**研究 1 → 步骤 1：瞬态** → **输出时步**：

```
range(0,60,300000)
```

（每 60 s 一点，覆盖 83 小时，足够）

## 3.3 计算

右键 **研究 1** → **计算**。

> 这一问计算量大（5000 个时间步 × 400 个单元），可能要跑几分钟。

## 3.4 求烘干时间

1. 展开 **结果** → **右键「派生值」** → 选 **点计算**
2. 设置窗口：
   - **表达式** = `C`
   - **点选择**：在图形窗口里点线段**最左端**那个点（r = 0，药材中心）
3. 点 **计算**，得到一张 C 随时间的表
4. 找 **C 第一次小于 0.15** 的时刻

> **为什么看中心点**：表面先干、中心最后干，中心降到 0.15 以下时全药材才都合格。
>
> **想更精确**：在跨越 0.15 的相邻两个时刻之间做线性内插，例如
> t₀ 时 C = 0.152、t₁ = t₀ + 60 s 时 C = 0.147，则
> t = t₀ + 60 × (0.152 − 0.15)/(0.152 − 0.147) 秒。

### ⚠ 3.4.1 一个必须避开的显示陷阱

中心含水率**恰好在 0.15 附近**时，"四位小数"会把结果四舍五入成 `0.1500`，
**看起来像没达标**。

实测数据（某次真实运行）：

| 时刻 | 中心 C 真实值 | 四位小数显示 | 判断 |
|---|---|---|---|
| 205620 s | 0.15001758 | 0.1500 | 超标 |
| 205680 s | **0.14999997** | **0.1500** | ← 实际达标，但**显示看不出来** |
| 206100 s | 0.149877 | 0.1499 | 显示达标 ✓ |
| **206400 s** | **0.149790** | **0.1498** | 显示达标 ✓（推荐） |

**建议做法：**

- **论文正文**可以写"中心含水率约在 205680 s（57.1 h）首次降至 0.15 以下"
- **但表 5 的最后一列/最后一行**，用一个显示上明确达标的时刻，比如
  **206400 s（57.3 h）**，此时中心 C = **0.1498**，不会有任何歧义
- 两者只差 12 分钟，对"2 ~ 3 天"的结论没有任何影响

**对应地，输出时步的终点写：**

```
range(60,60,206400)
```

（206400 ÷ 60 = 3440 ✓ 是整数倍，最后一行正好是这个时刻）

## 3.5 导出 result3

1. 把 **输出时步** 改成（`<烘干结束时刻>` 换成第 3.4 步算出的秒数）：

```
range(60,60,<烘干结束时刻>)
```

2. 右键 **研究 1 → 计算**
3. 导出：**数据集** = `研究 1/解 1`，**表达式** = `C`，**文件名** = `comsol_p3_C.csv`
4. 生成 xlsx：

```
python "C:\Users\Wil\Documents\ChatGPT\CUMCM\csv_to_result.py" comsol_p3_C.csv comsol_p3_C.csv result3.xlsx 20
```

> result3 只有一个工作表（水分浓度），所以温度 csv 那一栏随便填同一个文件即可；
> 更准确的做法是打开生成的 xlsx，**删掉多余的「水分浓度」工作表**，把「温度」表改名为 `Sheet1`。

## 3.6 填论文表 5

时间取 **6、12、18、…（小时）**，距离取 **0、0.5、1、1.5、2 cm**。
表格最后一行写 **烘干结束时间**。

---

# 第 4 章 问题 4：药材会缩水

## 4.0 目标

物性换成**附录 4**，并且**药财半径随时间收缩**（附件2）。求出这个条件下的烘干时长。

| 交付物 | 内容 |
|---|---|
| 论文表 6 | 每隔 6 h × 每隔 0.5 cm 的水分浓度，**最后一列是"药材表面"** |
| 论文正文 | 烘干时长（h） |
| result4.xlsx | 每隔 60 s、每隔 0.1 cm 的完整结果 |

## 4.1 另存文件

打开 `A_问题3.mph` → **文件 → 另存为** → `A_问题4.mph`

## 4.2 改动 1：物性换成附录 4

点 **固体传热 (ht) → 固体 1**：

| 设置项 | 表达式 |
|---|---|
| 导热系数 k | `0.12[W/(m*K)]+0.20[W/(m*K)]*C/(C+1)` |
| 密度 ρ | `760[kg/m^3]+90[kg/m^3]*C` |
| 比热容 Cp | `1850[J/(kg*K)]+2150[J/(kg*K)]*C/(C+1)` |

点 **定义 → 变量 1**，把 `D_coef` 改成：

```
4.2e-4[m^2/s]*exp(-0.30/C)*exp(-3850[K]/T)
```

## 4.3 改动 2：读入半径收缩曲线

1. 右键 **定义** → **函数 → 插值**
2. **数据源** = `文件`，**文件名** = `附件2.xlsx`，**数据格式** = `电子表格`
3. 「数据列设置」：

| 行 | 类型 | 名称 | 单位 |
|---|---|---|---|
| 列 1 | `变元` | （无名称栏） | `s` |
| 列 2 | `函数值` | `Rcm` | `1` |

4. **内插和外推 → 外推** = `常数` → 点 **导入**
5. 展开 **内插和外推** → 确认 **外推** = `常数`

## 4.4 改动 3：定义当前半径

点 **定义 → 变量 1**，新增一行：

| 名称 | 表达式 |
|---|---|
| `R_t` | `Rcm(t)*0.01[m]` |

（附件2 的半径单位是 cm，乘 0.01 换成 m）

## 4.5 改动 4：加变形几何（让药材表面收缩）

> **位置先记住**：COMSOL 6.3 的「变形几何」**不在「定义」下面**，而是和「固体传热」「系数型偏微分方程」并列的、**组件 1 下面的一个分支**。
> 而且**右键谁，决定你能拿到什么节点**，这是这一节全部坑的来源：
>
> | 你右键的对象 | 拿到的节点 |
> |---|---|
> | **组件 1** | 只有三个**域级**节点：变形域、指定变形、旋转域 |
> | **变形几何**（分支） | 完整集合，含**边界级**节点：指定变形、指定网格位移、固定网格…… |

### 第 1 步：加「变形域」（域级，必须）

1. 在模型开发器里**右键「组件 1」**
2. 选 **变形几何** → **变形域**（English: Deforming Domain）
3. 保持默认：选择 = **所有域**

作用：域的**内部**网格交给平滑方程，跟着边界一起动。

### 第 2 步：加「指定变形」（边界级，决定表面缩到哪）

1. 此时「组件 1」下面出现了 **变形几何** 分支
2. **右键「变形几何」这个分支**（不是右键组件 1）→ **指定变形**（English: Prescribed Deformation）
3. 在这个「指定变形 1」的设置里依次填：

| 设置项 | 填什么 |
|---|---|
| 几何实体层 | **边界** |
| 选择 | **边界 2**（r = 0.02 那个端点） |
| 指定变形 | **勾上 r 方向前面的复选框**，表达式填 `R_t-R_drug` |

4. 单位应自动是 `m`（橙色的说明表达式没被接受，先检查 `R_t` 是不是在「组件 1 → 定义 → 变量 1」里）

> **为什么必须在「变形几何」分支上右键**：右键「组件 1」时菜单只给三个域级节点，
> 你如果在那里加「指定变形」，它的「几何实体层」只能是域，硬选边界就会提示**实体不适用**。
> 边界级节点只从**「变形几何」分支**上右键才出得来。

### 第 3 步：把 r = 0 那一端钉住

1. 再次**右键「变形几何」分支** → **指定变形**（加出「指定变形 2」）
2. 设置里：几何实体层 = **边界**；选择 = **边界 1**（r = 0 那一端）；r 方向勾上，表达式填

```
0
```

> 替代做法：这一步也可以改加 **指定网格位移**（边界 1，各个方向的复选框都不勾，默认即零位移）。

### 检查点

- 「组件 1 → 变形几何」下面应有三个节点：**变形域 1（域）+ 指定变形 1（边界 2）+ 指定变形 2（边界 1）**
- 改完**必须重新点「计算」**
- 算完后画 `r` 坐标：最大值应随时间从 0.02 降到约 0.01198 m（附件2 最后是 1.198 cm）

## 4.6 改动 5：边界通量改成用当前半径

点 **系数型偏微分方程 (c) → 通量/源 1**：

**边界通量 g** 从 `R_drug*h_mass*(C_air-C)` 改成：

```
R_t*h_mass*(C_air-C)
```

**为什么**：半径在收缩，表面积跟着变，通量必须按**当前**半径算。

## 4.7 改动 6：时间范围

**研究 1 → 步骤 1：瞬态** → **输出时步**：

```
range(0,60,259200)
```

（每 60 s 一点，覆盖 72 小时）

## 4.8 计算与求烘干时长

1. 右键 **研究 1** → **计算**
2. 用与问题 3 相同的方法：**派生值 → 点计算**，`表达式` = `C`，点选 **最左端点（r = 0）**
3. 找 C 首次 < 0.15 的时刻 → 换算成小时

## 4.9 导出 result4

### 第一部分：内部各点

- **结果 → 导出 → 数据**：**数据集** = `研究 1/解 1`，**几何层** = `来自数据集`，**表达式** = `C`，**文件名** = `comsol_p4_C.csv`

### 第二部分：药材表面那一列

> **先纠正**：COMSOL 6.3 的数据集菜单里**没有单独的「点」**（`一维栅格` 也不行，它算不了场变量）。
> 单点数据集的名字叫 **切割点 1D**（English: Cut Point 1D），在 **右键「结果 → 数据集」→ 更多数据集** 里。
>
> **但问题 4 不要用切割点**：切割点必须填**固定坐标**（例如 r = 0.02），而问题 4 的表面在收缩，
> t > 0 时 r = 0.02 已经跑到材料域外面了，导出来是 NaN。
> 要取"当前表面"，必须用**跟着几何实体走**的方式。

#### 方案 A（推荐）：派生值 → 点计算

1. **右键「结果」→ 派生值 → 点计算**（Point Evaluation）
2. 选择 = **边界 2**（图形窗口里点最右端那个端点；或先看「几何实体层」，在列表里勾 2）
3. 表达式区填：**表达式** = `C`，**单位** = `1`（无量纲）
4. 点设置窗口顶部的 **计算** 按钮 → 结果出现在 **结果 → 表格** 下
5. **右键那个表格 → 添加表格导出**（Add Table to Export）
6. 点中 **结果 → 导出 → 表格 1**：**文件名** = `comsol_p4_surface.csv`，类型 = CSV → 点 **导出**

> 表格里就是"每一个保存时刻一行的表面 C 值"，直接给 `csv_to_result.py` 或手工贴进 result4.xlsx 都行。

#### 方案 B（更快，复用现有导出）：把「几何层」改成"点"

1. 点中已有的 **结果 → 导出 → 数据 1**
2. 把 **几何层**（Geometry level）从 `来自数据集` 改成 **`点`**（Point）
3. **文件名** 改成 `comsol_p4_surface.csv`，点 **导出**
4. 这样每个时步会输出**两个端点**（r = 0 和 r = R_t）的 C，看坐标列取 r 大的那一个就是药材表面

> 用完记得把「数据 1」的几何层改回 `来自数据集`，否则后面再导内部数据会串。

## 4.10 生成 result4.xlsx

```
python "C:\Users\Wil\Documents\ChatGPT\CUMCM\csv_to_result.py" comsol_p4_C.csv comsol_p4_C.csv result4.xlsx 20
```

生成后打开 xlsx，**把「药材表面」那一列补上**（用 `comsol_p4_surface.csv` 里的值），并删掉多余工作表。

## 4.11 填论文表 6

时间取 **6、12、18、…（小时）**，距离取 **0、0.5、1、1.5、2 cm**，**最后一列是"药材表面"**（随时间变化的半径处的值）。

---

# 第 5 章 论文配图怎么画

## 5.1 先想清楚：题目不要求画图

四问的题目只要求"论文表格 + result 文件"，**没有任何一句要求画图**。
图是论文自己要用的，每问 **2 ~ 3 张**就够。

## 5.2 画图卡的根源与解决办法

根源是**每个时间步画一条曲线**：

| | 时间步数 | 时间选择=「全部」时画几条曲线 |
|---|---|---|
| 问题 1 | 1801 | 1801 条 |
| **问题 2** | **10801** | **10801 条** ← 卡死的元凶 |
| 问题 3 | 5001 | 5001 条 |

**解决办法：改绘图组的「时间选择」。**

1. 点中绘图组（不是下面的线图）
2. 设置窗口的「**数据**」区 → **时间选择**
3. 三个选项：

| 选项 | 效果 |
|---|---|
| `全部` | 卡（默认值） |
| **`最后一个`** | 只画 1 条，最快 |
| **`手动`** + 填 `range(起点,间隔,终点)` | 只画指定的几条 ← **推荐** |

> **这只影响画图，不影响求解和导出。** 导出的 csv 照样是全部时间步，
> result*.xlsx 也照样是完整数据。

### 5.2.1 图已经卡住了，改不动设置怎么办

问题 2 的绘图组一选中就会自动重绘（10801 条曲线），**卡到没法去改「时间选择」**。
解决办法是**先把自动绘图关掉**：

1. 展开 **结果** → **点中「结果」节点本身**（不是下面的绘图组）
2. 在设置窗口里找到 **「仅当请求时绘制」**（英文 *Only plot when requested*）
3. **勾选它** → 之后选中绘图组**不会自动重绘**
4. 现在从容地去改绘图组的「时间选择」：
   - 点中绘图组 → 「数据」区 → **时间选择** = `手动`
   - 「时间」填 `range(1800,1800,10800)`
5. 改完再点 **绘制**（或按 `F8`），只画那 6 条

> 想恢复自动绘图：把「仅当请求时绘制」的勾去掉即可。
>
> 也可以在 **首选项 → 结果 → 自动更新绘图** 里，勾选「为新模型禁用」，
> 这样以后新建的模型默认就不会自动绘图。

### 5.2.2 如果卡到 COMSOL 完全没反应

- 等一两分钟，它可能自己画完
- 还不行就**强制结束 COMSOL 进程**，重新打开 `.mph` 文件（结果都在文件里，不会丢）
- 打开后**先做 5.2.1 的第 1~3 步**，再去碰绘图组

## 5.3 图一：沿半径的分布图

横轴半径、纵轴温度或水分浓度、若干条曲线代表不同时刻。

1. 展开 **结果** → **右键「结果」** → **一维绘图组**
2. 在「一维绘图组 1」上**右键** → **线图**
3. 点中「**一维绘图组 1**」（不是线图），在「**数据**」区设：
   - **数据集** = `研究 1/解 1`
   - **时间选择** = **`手动`**
   - **时间** = 见下表
4. 点中「**线图 1**」，**表达式** 填 `C`（或 `T_C`）
5. 点顶部 **绘制**

**每问的「时间」填什么：**

| 问题 | 填 | 画出几条 |
|---|---|---|
| 问题 1 | `100 300 600 900 1200 1500 1800` | 7 条（对应表 1、表 2） |
| **问题 2** | **`range(1800,1800,10800)`** | **6 条**（0.5、1、1.5、2、2.5、3 h） |
| 问题 3 | `range(21600,21600,<烘干结束秒数>)` | 每隔 6 h 一条 |
| 问题 4 | 同上 | 同上 |

> 如果「手动」里写数字列表报语法错，就改用 `range` 形式，
> 例如问题 1 用 `range(300,300,1800)` 也能画出 6 条。

## 5.4 图二：随时间变化图（重点，且不卡）

这张图说明"烘干快慢"，只有 **1 条曲线**，完全不卡。

1. **右键「结果 → 数据集」→ 更多数据集 → 切割点 1D**（Cut Point 1D）
2. 坐标填 `0`（药材中心）
3. **右键「结果」→ 一维绘图组** → 在它下面 **右键 → 线图**
4. 点中「线图 1」：**数据集** = 上一步的「切割点 1D 1」，**表达式** = `C`
5. 点中「**一维绘图组**」，在「**轴**」区把 **x 轴数据** 从 `弧长` 改成 **`时间`**
6. 点 **绘制** → 得到一条 C(中心, t) 曲线

> 想同时看中心和表面：再建一个「切割点 1D 2」放在 `r = 0.02`（问题 1~3 的边界不动，坐标法可行），
> 然后在同一绘图组里加第二条线图，数据集选它。

## 5.5 每问建议配图

| 问题 | 建议画 |
|---|---|
| 问题 1 | ① 30 min 温度分布（7 条）② 30 min 水分分布（7 条） |
| **问题 2** | ① 3 h 温度分布（6 条）② 3 h 水分分布（6 条）③ **中心点水分随时间变化** |
| 问题 3 | ① 每隔 6 h 的水分分布 ② **中心点水分随时间变化**（标出 0.15 那条水平线） |
| 问题 4 | ① 每隔 6 h 的水分分布 ② **中心点水分随时间变化** ③ 半径随时间收缩（附件2 数据） |

**"中心点水分随时间变化"这张是重点**——问题 3、4 的答案（烘干时长）
就是从这张图读出来的，务必画。

---

# 第 6 章 出口检查清单

每做完一问，逐条打勾：

- [ ] 物性表达式已换成该问对应的附录公式（含单位）
- [ ] `D_coef` 已换成该问的公式（指数里 T 用 `[K]`）
- [ ] `da = r`、`c = r*D_coef` **没被改掉**
- [ ] 源项物理量单位 = `m/s`
- [ ] `g` 用的是正确的半径（问题 1–3 用 `R_drug`，问题 4 用 `R_t`）
- [ ] 改完之后**点了「计算」**（不是只改不跑）
- [ ] 导出的 csv 头两行能看到表头和数据
- [ ] 1800 s（或对应时刻）的数值物理上合理
- [ ] result*.xlsx 两个工作表都有数据、时间从 1 或 60 开始、四位小数

---

# 第 7 章 排错表

| 现象 | 原因 | 处理 |
|---|---|---|
| 系数 `c` 报单位错 | 只改了系数没改「单位」区 | 先把源项物理量单位改成 `m/s`，系数表里 `c` 的期望单位会自动变成 m³/s |
| 扩散系数那一栏写死 `m²/s` 改不动 | 单位列可以手动编辑 | 直接点那一格输入 `m^3/s` |
| 导出报"未定义变量 comp1.T_C / 全局范围" | 数据集选成了"一维栅格"，或改完没重算 | 数据集改成 `研究 1/解 1`；并重新「计算」 |
| 一维绘图组画出来是空的 | 线图的"选择"里少了实体 | 把选择改成 **所有域** |
| 插值报"未编号的行被忽略" | 第 1 行是表头 | 无害，忽略 |
| 找不到「变形几何」（以为在「定义」里） | 6.3 的变形几何是**组件 1 下的分支**，不是「定义」的子节点 | 右键「组件 1」→ 变形几何 → 变形域；边界级节点要**右键「变形几何」分支**，见 4.5 节 |
| 变形几何里提示"边界 2 不适用"、实体选不上 | **节点加错了地方**：右键「组件 1」只能拿到域级节点，它的几何实体层只有「域」 | 删掉这个节点，改从**「变形几何」分支**上右键加「指定变形」，再把几何实体层改成「边界」 |
| 认为「`R_drug` 是参数不是变量，所以填不进去」 | 误判。参数和变量在表达式里**可以随便混用** | 名字写对就不会报错；真出错会写「未定义变量」，不会写「不适用」 |
| 位移表达式的 `R_drug` 拼成了 `R_drag` | 参数名拼错 | 去「全局定义 → 参数 1」核对第一行的真实名字，四处（几何上界、g、位移、R_t 无关）都要一致 |
| 导出的水分比独立算法偏高 3.5% | 漏了圆柱权重 r | 检查 `da = r`、`c = r*D_coef`、`g = R*h_m*(C_air-C)` |
| **画图卡到动不了，设置都改不了** | 绘图组的「时间选择」是「全部」，要画上万条曲线；一选中就自动重绘 | 先点中「结果」节点勾选「**仅当请求时绘制**」，再去改「时间选择」，见第 5.2.1 节 |
| 求解慢（不是画图慢） | 网格太密或时间步太多 | 网格可降到 200 个单元；时间步按题目要求不能改 |
