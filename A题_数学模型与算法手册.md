# A 题《药材的烘干问题》数学模型与求解/验证算法手册

> 本手册的用途有两个：
> 1. **写论文**：第 1、2、3、4、5 章就是"模型建立"与"模型求解"的正文素材，第 8 章是"模型的验证与误差分析"。
> 2. **看懂数据**：第 6、7 章解释每一步数值算法到底在算什么，第 8 章解释我是**怎么验证你的 result1~4.xlsx** 的。
>
> 阅读顺序建议：先看第 1 章（公共方程），再按题号看第 2~5 章，最后看第 8 章（验证）。
>
> **第二版新增的讲解约定**：手册里每一处**新的概念 / 公式 / 算法**后面，都紧跟一段以 `>` 开头的引用块。这种引用块是"像给大学生上课一样"的讲解，尽量讲清三件事——**说的是什么**（把式子翻译成一句人话）、**为什么成立**（背后的物理或数学依据）、**怎么用、坑在哪**（论文里怎么表述、计算时注意什么）。引用块本身**不是论文正文**：你要抄进论文的是它上面那些公式和表格，引用块是留给你自己的"说明书"。

---

## 阅读地图：一条主线串起四问

整篇论文其实只有**一条主线**，四问是这条主线上一层层加上去的复杂度。先记住这句话：

> **共同骨架**：药材内部的水分靠扩散往外走，热量靠导热往里走，表面同时和烘房空气交换热量与水分。四问的差别只在"物性取哪套公式""药材会不会变小""要不要找停止时刻"这三件事上。

| 层次 | 相对上一层新增了什么 | 对应问 | 手册章节 | 论文里通常放在 |
|---|---|---|---|---|
| 骨架 | 一维轴对称扩散 + 第三类边界条件 | 公共 | 第 0、1 章 | 模型假设、模型建立 |
| 第 1 层 | 物性取常数（附录 2），温度与水分基本解耦 | 问题 1 | 第 2 章 | 模型建立（情形一） |
| 第 2 层 | 物性随 $C,T$ 变化（附录 3），方程由线性变成拟线性耦合 | 问题 2 | 第 3 章 | 模型建立（情形二） |
| 第 3 层 | 加一个"达标判据"，把问题变成求时间 | 问题 3 | 第 4 章 | 模型求解与结果 |
| 第 4 层 | 边界会动（半径收缩），改用材料坐标 | 问题 4 | 第 5 章 | 模型建立（情形三） |
| 工具 | 怎么把方程真正算出来 | 公共 | 第 6 章 | 模型求解（算法说明） |
| 体检 | 怎么证明算得对 | 公共 | 第 7、8 章 | 模型验证、误差分析 |
| 成文 | 表格怎么填、图怎么画、文件怎么交 | 公共 | 第 9 章与附录 | 结果展示 |

> **写论文时的一条建议**：不要四问各写一套毫不相干的模型，而是"先立骨架（第 0、1 章），再逐层加复杂度"。审阅人看到的就是**一个统一的模型在四种情形下的退化与推广**，逻辑会清楚很多，也更容易看出你的建模功力。反过来，如果四问各写一套，读者会以为是四个不同的课题拼在一起。
>
> **这条主线对应到论文结构**：第 0 章的三条假设 → "模型假设"；第 1 章的方程组 → "模型建立"第一步；第 2~5 章 → "模型建立与求解"的四小节；第 6 章 → "模型求解"里的算法说明；第 8 章 → "模型验证与误差分析"；第 9 章 → "结果分析"。你可以按自己的行文顺序重排，但**骨架顺序不要打乱**，因为它就是"由简到繁"的论证逻辑。

---

## 第 0 章 统一约定与基本假设

### 0.1 几何与坐标系

药材为圆柱体：半径 $R=0.02\ \mathrm{m}$，长度 $L=0.25\ \mathrm{m}$。
取圆柱中轴为对称轴，用柱坐标 $(r,\theta,z)$，其中 $r$ 为**到药材中心的距离**（0 在轴心，$R$ 在表面）。

题目表格给出的量都是"到药材中心的距离 $0,0.5,1,1.5,2$ cm 处"的值，说明关注的是**径向分布**，因此采用**一维轴对称模型**（只保留 $r$ 方向）。

**为什么可以降为一维：**

- 长径比 $L/(2R)=25/4=6.25$，轴向导热/扩散的特征长度远大于径向，径向导数远大于轴向导数；
- 题目只要求给出径向不同位置的温度和含水率；
- 端面（两个圆面）面积与侧面面积之比为
  $$\frac{2\pi R^2}{2\pi R L}=\frac{R}{L}=\frac{2}{25}=8\%$$
  即端面贡献约 8%，属于模型假设范围，可在论文"模型假设/局限性"中明确写出（若要把端面算进去，只需在边界条件里把总换热/传质面积写成 $2\pi R L+2\pi R^2$ 的等效形式）。

> **"到药材中心的距离"这个说法必须先钉死**：题面里的 0、0.5、1、1.5、2 cm 不是"从表面往里的深度"，而是**从轴心往外量的半径**。这一点不统一，后面所有表格都会左右颠倒。论文里建议在模型假设之后立刻写一句："本文以圆柱中轴为对称轴，$r$ 表示到药材中心的径向距离，$r=0$ 位于轴心，$r=R$ 位于药材表面。"
>
> **为什么可以用柱坐标而不是直角坐标**：圆柱关于中轴旋转对称，同一半径上的所有点状态完全相同，角度 $\theta$ 因此可以从方程里彻底消失。这叫**对称性降维**——它不是"把问题简化错了"，而是精确地利用了问题本身的对称性，所得方程与三维方程在这一意义下完全等价。
>
> **为什么可以再降到一维**：判据是"两个方向的扩散时间谁短"。扩散走完距离 $\ell$ 所需的时间约为 $\ell^2/D$：径向要走 2 cm，轴向最长要走 12.5 cm（半长），时间之比为 $(2/12.5)^2\approx2.6\%$。也就是说，轴向上出现明显水分差所需的时间比径向长近 40 倍。因此在烘干前中期（几十小时内）轴向的导数远小于径向导数，"一维径向模型"是很自然的近似。
>
> **端面那 8% 怎么解释**：圆柱只有两个端面（圆面）暴露，面积为 $2\pi R^2$；侧面面积为 $2\pi RL$。端面与侧面之比为 $R/L=2/25=8\%$。忽略端面意味着低估了约 8% 的换热与传质面积，**这是模型误差而不是计算误差**（加密网格也不会让它变小），必须写进论文的"模型假设与局限性"。同时也要给出它可接受的理由：本问只关心径向分布，且长径比 $L/(2R)=6.25$ 较大。
>
> **一个可以用在论文里的写法**："考虑到药材为细长圆柱（长径比 6.25），端面面积仅占侧面面积的 8%，且题目只要求给出径向分布，本文采用一维轴对称模型；端面效应作为模型局限性在误差分析中说明。"

### 0.2 符号表（全篇统一）

| 符号 | 含义 | 单位 |
|---|---|---|
| $r$ | 到药材中心的距离（径向坐标） | m |
| $t$ | 时间 | s |
| $T(r,t)$ | 药材温度 | K（表格用 ℃） |
| $C(r,t)$ | 干基含水率（水分浓度） | kg/kg |
| $\rho(C)$ | 药材密度 | kg/m³ |
| $c_p(C)$ | 定压比热容 | J/(kg·K) |
| $k(C)$ | 导热系数 | W/(m·K) |
| $D(C,T)$ | 水分扩散系数 | m²/s |
| $h=25$ | 对流换热系数 | W/(m²·K) |
| $h_m=8\times10^{-7}$ | 对流传质系数 | m/s |
| $T_{air}(t),C_{air}(t)$ | 烘房温度、水分浓度（附件 1） | K，kg/kg |
| $R_t$ | 问题 4 中随时间变化的药材半径（附件 2） | m |
| $T_0=301.15$ | 初始温度（28 ℃） | K |
| $C_0=2.55$ | 初始含水率 | kg/kg |
| $L=0.25,\ R_0=0.02$ | 药材长度、初始半径 | m |
| $R(t)$（代码里写 $R_t$） | 问题 4 中随时间收缩的药材半径，$R(0)=R_0$ | m |
| $X$ | 材料坐标：某材料点**初始时刻**到轴心的距离，$X\in[0,R_0]$ | m |
| $\rho_s$ | 单位体积内的干物质质量（推导中间量，最后约掉） | kg/m³ |
| $M(t)$ | 时刻 $t$ 药材内部的**最大**含水率 $\max_r C(r,t)$ | kg/kg |
| $t_{dry}$ | 烘干所需时间（各处首次全部低于 0.15 kg/kg 的时刻） | h |
| $\ell$ | 扩散穿透深度（扩散长度），$\ell\sim\sqrt{Dt}$ | m |
| $D_0$ | 初始状态下（$C_0,T_0$）的扩散系数 | m²/s |
| $Bi_m=h_mR/D_0$ | 传质毕渥数（内部扩散阻力 ÷ 外部对流传质阻力） | — |
| $\tau_D=R^2/D_0$ | 扩散特征时间 | s |

> **"干基含水率"到底是什么，为什么非要区分它**：$C$ 的定义是 $C=m_w/m_d$，即**水分质量 ÷ 干物质质量**，单位 kg/kg。这与生活里说的"含水量"（$m_w/(m_w+m_d)$，永远小于 1）不是一回事：干基含水率可以大于 1。本题初始值 2.55 的意思是"1 kg 干药材里带着 2.55 kg 水"。论文里第一次出现时**必须写出这个定义式**，因为后面所有守恒方程用的都是 $\rho_s C$（干物质质量 × 干基含水率 = 水分质量），而不是 $\rho C$。
>
> **为什么守恒推导里用 $\rho_s$ 而符号表里用 $\rho$**：$\rho$ 是题目给的"含水药材的整体表观密度"，它随含水率变化；$\rho_s$ 是单位体积里的干物质质量，它在干燥过程中**不变**（干物质不会进出），所以最适合当记账单位。两者关系是 $\rho_s=\dfrac{\rho}{1+C}$。**在本题中不需要真的去算这个换算**——$\rho_s$ 只是推导过程中的中间量，最后会被约掉（见 1.2 节）。论文里只需说明"$\rho(C)$ 为题目给出的湿药材表观密度"即可。
>
> **符号表为什么值得单独列一节**：竞赛论文的评委是"跳着看"的。当他看到式 (15) 里出现 $R_0$ 和 $R(t)$ 时，第一反应是回翻符号表。符号表里没有这两个量，读者就会卡住。**判断符号表是否合格的标准很简单：把正文里每一个符号都圈出来，看能不能在表里找到。**

### 0.3 基本假设（建议原样写进论文）

| 序号 | 假设 | 理由与影响 |
|---|---|---|
| H1 | 药材内部各向同性、连续介质，局部热平衡（同一位置固相与水分温度相同） | 药材尺寸小、导热快，$R^2/\alpha\approx39$ min，热平衡时间远小于烘干时间 |
| H2 | 水分迁移服从 Fick 扩散定律，扩散系数 $D$ 由题目经验公式给定 | 题目直接给了 $D$ 的经验式，无需另建机理模型 |
| H3 | 热量传递由导热控制，表面对流按牛顿冷却定律 | 题目给了 $h$，正文用第三类边界条件 |
| H4 | 一维轴对称（忽略轴向与端面） | 见 0.1，端面影响约 8% |
| H5 | 问题 1~3 中几何尺寸不变 | 只有问题 4 给出半径变化数据 |
| H6 | 水分只在表面与烘房空气交换，轴心为对称面（无通量） | 圆柱几何的天然对称性 |
| H7 | 物性只依赖当前含水率（和温度），与历史无关 | 题目经验公式的形式 |
| H8 | 问题 4 中收缩为**均匀（线性）收缩**：材料点到轴心的相对位置不变 | 与"变形域"平滑方程在 1D 下的解一致；也便于与文献中的干燥收缩模型对照 |

> **假设不是走过场，每一条后面都得"用得上"**：好的假设段落有一个特征——正文里能明确写出"正是因为 H4，我们才可以把方程写成一维"。建议按下面的对应关系组织你的"模型假设"一节：
>
> | 假设 | 它支撑了后面哪一步 |
> |---|---|
> | H1 局部热平衡 | 支撑"用 $\rho c_p T$ 表示显热"、不必分固相与水两套温度 |
> | H2 Fick 定律 | 支撑扩散项的具体形式 $\nabla\cdot(D\nabla C)$ |
> | H3 牛顿冷却 | 支撑表面对流边界条件 (3)(4) 的形式 |
> | H4 一维轴对称 | 支撑方程里没有 $\theta$、$z$ 两个方向 |
> | H6 轴心对称 | 支撑 $r=0$ 处的零通量条件 (5) |
> | H7 物性只依赖当前状态 | 支撑把 $\rho,c_p,k,D$ 写成 $f(C,T)$ 而不带历史项 |
> | H8 均匀收缩 | 支撑问题 4 里 $r=X\,R(t)/R_0$ 这个运动学关系式 (13) |
>
> **反过来的自查方法**：如果某条假设在后文找不到任何"被它支撑的步骤"，那它就是凑字数，删掉比留着更安全——写得越多，被质疑的面越大。
>
> **H1 里那句"热平衡时间远小于烘干时间"是怎么估的**：把热量传遍半径 $R$ 所需的时间记为 $\tau_T\sim R^2\rho c_p/k\approx39$ min，而烘干要几十小时，两者差两个数量级。所以可以放心地认为"固相和水随时同温"。**注意这里给的是一个"时间尺度比较"的论证，而不是一个精确等式**——论文里这样写就够了，不需要证明。

---

## 第 1 章 控制方程的推导（四问共用）

### 1.1 取微元

在半径 $r$ 处取一个厚度 $\mathrm{d}r$、高度 $L$ 的圆柱薄壳（图 1）：

- 内表面积 $A(r)=2\pi rL$
- 体积 $\mathrm{d}V=2\pi rL\,\mathrm{d}r$

两侧（$r$ 与 $r+\mathrm{d}r$）是通量交换面，其余面为对称面。

> **"取微元"是守恒建模的通用套路**：不去看整根药材，只盯住一小块（这里是高度为 $L$ 的薄圆筒壳），然后对**所有进出这一小块的东西记账**。为什么这样可以导出偏微分方程？因为"薄壳上的收支平衡"对任意一块薄壳都成立；令壳厚 $\mathrm{d}r\to0$，就得到一个逐点成立的微分方程。这是三大守恒定律变成 PDE 的**唯一入口**，论文里可以照这个逻辑写：选控制体 → 列守恒 → 令尺寸趋于零。
>
> **注意两个表面积不相等**：内表面是 $2\pi rL$，外表面是 $2\pi(r+\mathrm{d}r)L$，二者差一个 $\mathrm{d}r$ 量级。**这正是柱坐标方程里出现 $\frac{1}{r}\frac{\partial}{\partial r}(r\,\cdot\,)$ 的根本原因**——在直角坐标里两个面完全相等，所以那里是干净的二阶导数 $\partial^2/\partial r^2$。很多同学写圆柱问题时直接套用平板方程，就是漏掉了这一步。
>
> **写论文时的标准句式**："在半径 $r$ 处取厚度 $\mathrm{d}r$、高度 $L$ 的圆柱薄壳作为控制体，对其列出水分（能量）守恒式并令 $\mathrm{d}r\to0$，得……"记住这三句话的顺序：控制体、守恒、取极限。

### 1.2 水分守恒 → 水分方程

薄壳内水分（以干基表示）质量为

$$m_w=\rho_s C\,\mathrm{d}V$$

其中 $\rho_s$ 为单位体积对应的干物质质量（常数）。单位时间从内表面流入、外表面流出的水分由 Fick 定律给出

$$\mathbf{J}=-D\,\frac{\partial C}{\partial r},\qquad
Q(r)=-D\frac{\partial C}{\partial r}\cdot 2\pi rL\rho_s$$

质量守恒（流入 − 流出 = 累积）：

$$\frac{\partial}{\partial t}\left(\rho_s C\,2\pi rL\,\mathrm{d}r\right)
= -\left[Q(r+\mathrm{d}r)-Q(r)\right]$$

两边除以 $\rho_s 2\pi L\,\mathrm{d}r$ 并令 $\mathrm{d}r\to0$：

$$\boxed{\;\frac{\partial C}{\partial t}
=\frac{1}{r}\frac{\partial}{\partial r}\left(rD(C,T)\frac{\partial C}{\partial r}\right)\;}\tag{1}$$

> **Fick 定律在说什么**：$\mathbf{J}=-D\dfrac{\partial C}{\partial r}$。它说的是——扩散的**驱动力是浓度梯度**，方向永远从浓到稀（所以有负号），比例系数就是扩散系数 $D$（单位 m²/s，因为"通量 ÷ 梯度"的量纲是 $\frac{\mathrm{kg}/(\mathrm{m^2\cdot s})}{\mathrm{kg/m^4}}$）。它和"热量从热的地方流向冷的地方"（Fourier 定律）是完全平行的规律：**一个是物质搬家，一个是能量搬家**。论文里把它作为题目给定的**本构关系**（物理规律）引用即可，不需要证明。
>
> **逐项读方程 (1)**：左边 $\partial C/\partial t$ 表示"这一点的含水率变化有多快"；右边表示"流入减流出"的净效果。把它拆成三层看就清楚了——最里面的 $D\dfrac{\partial C}{\partial r}$ 是**通量**；乘上 $r$ 是把通量折算到"面积"上（柱面面积正比于 $r$）；外层的 $\dfrac{1}{r}\dfrac{\partial}{\partial r}$ 做的是"单位体积的净流出量"。三个动作连起来就是"单位体积的水分在一个微小时间内少掉多少"。
>
> **为什么说这个式子是"守恒"的**：把 (1) 两边同乘 $r\,\mathrm{d}r$ 再从 $0$ 积到 $R$，右端会变成一个**完全由边界值决定**的量（见 8.4 节的式 (21)）。也就是说，**药材内部总水量的变化只由表面通量决定**，内部无论怎么折腾都不可能凭空产生或消灭水分。这条积分恒等式就是后面验证数据的"金标准"。
>
> **$\rho_s$ 在这里为什么能约掉**：薄壳内的水分质量写成 $\rho_s C\,\mathrm{d}V$，两边同除以 $\rho_s 2\pi L\,\mathrm{d}r$ 时 $\rho_s$ 被整除掉了。所以水分方程最终不含 $\rho_s$——**这也是干基含水率这个定义的方便之处**：水的"存量"直接与干物质挂钩，而干物质质量守恒，可以放心当分母。
>
> **写作提示**：论文里最好把"物理规律"和"守恒分析"分成两步来写——"由 Fick 定律 $\mathbf{J}=-D\nabla C$，结合圆柱壳微元的水分守恒分析可得式 (1)"。这样审阅人一眼就能看出每个式子的来源。

### 1.3 能量守恒 → 温度方程

薄壳内显热为 $\rho c_p T\,\mathrm{d}V$，热流由 Fourier 定律给出

$$Q_T(r)=-k\frac{\partial T}{\partial r}\cdot 2\pi rL$$

同理得

$$\boxed{\;\rho(C)c_p(C)\frac{\partial T}{\partial t}
=\frac{1}{r}\frac{\partial}{\partial r}\left(rk(C)\frac{\partial T}{\partial r}\right)\;}\tag{2}$$

> **Fourier 定律与 (2) 的对应关系**：$\mathbf{q}=-k\nabla T$ 与 Fick 定律**形状完全相同**，只是把 $C$ 换成 $T$、把 $D$ 换成 $k$（单位也随之从 m²/s 变成 W/(m·K)）。这就是为什么水分方程和温度方程看起来"像一对双胞胎"——它们描述的是两个不同的物理量，但服从同一种"梯度驱动 + 守恒"的数学结构。**抓住这个对称性，你就不需要单独记两个方程了。**
>
> **为什么 (2) 左边多了 $\rho c_p$，而 (1) 没有**：因为"热量的存量"是 $\rho c_p T$（单位体积热容 × 温度），必须出现在时间导数前面；而"水分的存量"在干基表述下是 $\rho_s C$，而 $\rho_s$ 是常数、可以约掉。所以水分方程是"浓度型"，温度方程是"能量型"。这只是写法上的差别，物理上没有主次之分。
>
> **先对 $D$ 的量级有个印象（问题 1 的经验公式）**：$D=7\times10^{-9}e^{-0.89/C}$，它在 $C=2.55$ 时约 $4.9\times10^{-9}$，$C=0.5$ 时约 $1.2\times10^{-9}$，$C=0.15$ 时只有约 $1.9\times10^{-11}$——前后相差约 260 倍。**这就是"干燥越到后期越慢"的数学根源**，也解释了为什么问题 3、4 的时间跨度从几小时拉长到几十小时。论文里讨论"为什么最后 0.2 kg/kg 的水那么难除"时，可以直接引用这个量级对比。

### 1.4 边界条件

**外表面 $r=R$（第三类边界条件）**

对流换热：单位面积热流 $q=h\left(T_{air}(t)-T(R,t)\right)$，由 Fourier 定律 $q=-k\partial T/\partial r$ 得

$$\boxed{\;-k\frac{\partial T}{\partial r}\Big|_{r=R}=h\left(T(R,t)-T_{air}(t)\right)\;}\tag{3}$$

对流传质：单位面积水分通量 $j=h_m\left(C_{air}(t)-C(R,t)\right)$，由 Fick 定律 $j=-D\partial C/\partial r$（以干基浓度计）得

$$\boxed{\;-D\frac{\partial C}{\partial r}\Big|_{r=R}=h_m\left(C(R,t)-C_{air}(t)\right)\;}\tag{4}$$

**轴心 $r=0$（对称性）**

$$\frac{\partial T}{\partial r}\Big|_{r=0}=0,\qquad
\frac{\partial C}{\partial r}\Big|_{r=0}=0\tag{5}$$

（因为 $r=0$ 处面积为零，不存在通量。）

> **第三类边界条件是什么**：也叫**对流边界条件**或 **Robin 条件**。它不直接规定表面温度等于多少，而是规定**表面处的热量交换速率**：空气与表面的温差越大、换热越快，比例系数就是对流换热系数 $h$（本题 25 W/(m²·K)）。它与"第一类（直接给定表面温度）""第二类（给定表面热流）"并列为三类边界条件。论文里写"药材表面对流换热采用第三类边界条件"即可。
>
> **等式 (3) 两边的含义要读准**：左边 $-k\dfrac{\partial T}{\partial r}\Big|_{r=R}$ 是"热量靠**导热**到达表面的速率"，右边 $h\left(T(R,t)-T_{air}(t)\right)$ 是"热量被**空气带走**的速率"。二者相等背后的物理内核是——**表面没有厚度，不可能积累热量**，所以进多少就必须出多少。水分方程 (4) 完全同理，只是把温度换成含水率、把 $h$ 换成对流传质系数 $h_m$。
>
> **符号一定不能搞错**：写成 $h(T_{air}-T)$ 还是 $h(T-T_{air})$ 决定了正负号。唯一的判断方法就是**回到物理**：若 $T_{air}>T$，热量应当**流进**药材，此时表面处温度沿半径向外递增（$\partial T/\partial r>0$），前面加负号后 $-k\partial T/\partial r$ 应为正。本题采用 (3) 的写法，所有程序都必须与它保持一致。
>
> **$r=0$ 的条件 (5) 是结论而不是假设**：柱坐标下 $r=0$ 处的"面积"为零，任何有限通量乘以零面积都得零；数学上则是因为场量在轴心必须光滑，否则 $\frac{1}{r}\frac{\partial}{\partial r}(\,\cdot\,)$ 会出现奇性。所以论文里写"由轴对称性，轴心处为绝热（零通量）面"就够了，**不要**写成"假设轴心不透水"——那会让人觉得你不清楚这条条件的来源。
>
> **一个常见的概念混淆**：边界条件 (3) 与 (4) 是**耦合的**——表面温度 $T(R,t)$ 会通过 $D(C,T)$ 影响水分扩散，而含水率又通过 $\rho c_p(C)$ 影响温度。所以"表面"是四个物理过程（外部对流换热、外部对流传质、内部导热、内部扩散）交汇的地方，也是整个模型里最需要仔细处理的位置。

### 1.5 初始条件

$$T(r,0)=T_0=301.15\ \mathrm{K},\qquad C(r,0)=C_0=2.55\ \mathrm{kg/kg}\tag{6}$$

> **"定解问题"这个词的分量**：只有偏微分方程是不够的——同一个方程配上不同的初始/边界条件会得到完全不同的解。所谓"定解"，就是**方程 + 初始条件 + 边界条件三者齐全**，此时解才被唯一确定。写论文描述模型时，务必把这三样都写全；审阅人第一眼看的就是这三样是否齐全。
>
> **为什么初始温度写成 301.15 K 而不是 28 ℃**：两者严格等价，但后面温度相关经验式里含 $e^{-3850/T}$，要求 $T$ 用开尔文。**全文内部计算统一使用 SI 单位（K），只在输出表格时换算成 ℃**，这是避免量纲错误最省心的做法。28 ℃ = 301.15 K；反过来 ℃ = K − 273.15。
>
> **初始含水率为什么处处相同**：题目只给了"烘干开始时水分浓度为 2.55 kg/kg"这一个数，没有给初始的空间分布，因此按均匀分布处理。这是**受数据可得性限制而做的假设**，论文里应当明写："假定初始时刻含水率在药材内均匀分布，$C(r,0)=C_0=2.55$ kg/kg。"不要在正文里假装这是理所当然的。

### 1.6 完整定解问题

式 (1)(2) 为控制方程，式 (3)(4)(5) 为边界条件，式 (6) 为初始条件，构成抛物型耦合初边值问题。方程 (2) 的系数 $\rho c_p$ 依赖 $C$，方程 (1) 的系数 $D$ 依赖 $C,T$，因此该问题是**拟线性耦合**的，需要用数值方法求解。

> **"拟线性"是什么意思**：如果方程里最高阶导数的系数只依赖自变量（$r,t$），方程叫**线性**；若系数还依赖未知量本身（这里的 $D(C,T)$、$\rho c_p(C)$），就叫**拟线性**。它的实际后果有两条：(1) **叠加原理失效**，不能把两个解相加；(2) 方程是**耦合**的——温度方程里含 $C$、水分方程里含 $C$ 和 $T$，必须同时求解。论文里写"控制方程构成拟线性抛物型耦合初边值问题"，就是在说这两件事。
>
> **"抛物型"是什么意思（这是个分类术语）**：二阶线性 PDE 按 $Au_{xx}+Bu_{xy}+Cu_{yy}+\cdots$ 的判别式分成椭圆型、抛物型、双曲型三类。扩散/导热方程属于**抛物型**，它的标志性特征是"有无限传播速度、但有强平滑效应"——初值中哪怕有尖角，一瞬间就会被抹平；同时解满足**极值原理**（最大值只能出现在初始时刻或边界上）。这条性质在 4.2 节判断"中心最湿"时会直接用到。
>
> **为什么用数值方法而不是求解析解**：只有当 $D$、$\rho c_p$、$k$ 全是常数、几何又简单时，才可能用分离变量法写出级数解。本题中 $D$ 依赖 $C$（问题 2 起还依赖 $T$），问题 4 的边界还在动，**不存在实用的解析解**，必须离散求解。这句说明在论文里很重要——它能回答"你为什么要花力气做数值仿真"这个必然会被问到的问题。
>
> **顺手记住这个"问题复杂度阶梯"**：常数系数（问题 1）→ 变系数拟线性（问题 2、3）→ 变系数 + 动边界（问题 4）。这个阶梯本身就是你论文"模型逐步深入"的叙事骨架。

### 1.7 无量纲化与特征数（论文里很加分）

取 $\tilde r=r/R,\ \tilde t=t/\tau,\ \tau=R^2/D_0$，其中 $D_0$ 是初始含水率下的扩散系数，可得

$$\frac{\partial C}{\partial \tilde t}
=\frac{1}{\tilde r}\frac{\partial}{\partial \tilde r}
\left(\frac{D}{D_0}\tilde r\frac{\partial C}{\partial \tilde r}\right)\tag{7}$$

特征数与四问的取值：

| 量 | 定义 | 问题 1 | 问题 2/3 | 问题 4 |
|---|---|---|---|---|
| 初始扩散系数 $D_0$ | 由经验公式在 $C_0,T_0$ 处计 | $4.94\times10^{-9}$ | $5.63\times10^{-9}$ | $1.05\times10^{-9}$ |
| 传质毕渥数 $Bi_m$ | $h_mR/D_0$ | 3.2 | 2.8 | 15.2 |
| 传热毕渥数 $Bi$ | $hR/k$ | 1.39 | — | — |
| 扩散时间 $\tau_D$ | $R^2/D_0$ | 22.5 h | 19.7 h | 106 h |
| 热扩散时间 $\tau_T$ | $R^2\rho c_p/k$ | 39 min | 约 0.8 h | 约 1.4 h |

**解读（可直接写进论文）：**

- $\tau_T\ll\tau_D$：温度很快达到准稳态，含水率变化是"慢过程"，这正是题目把问题 1 称为**预热平衡阶段**的原因；
- $Bi_m\sim1$：外部对流传质与内部扩散阻力**同量级**，即表面和内部都要建模，不能只取其一；
- 问题 4 的 $D_0$ 比问题 3 小 5 倍多，$\tau_D$ 达 106 h，但药材边干边收缩（$R$ 从 2 cm 降到 1.2 cm，$\tau_D$ 降为约 38 h），两个因素共同决定了 50~53 h 的烘干时长。

> **无量纲化到底有什么用**：把 $r,t$ 换成 $\tilde r=r/R$、$\tilde t=t/\tau$ 之后，方程的形式不变，但系数变成了一堆**无量纲数**。它的价值在于——**解只依赖这些无量纲数，而不依赖 $R,D,h$ 的具体数值**。也就是说，只要两个问题的 $Bi_m$ 与 $D/D_0$ 相同，它们的解就是同一个解（在无量纲意义下）。这使结论可以推广（"对任意半径的同类药材同样成立"），也让数值实验的次数可以大大减少。
>
> **Biot 数 $Bi_m$ 的物理读法（本手册最重要的一个特征数）**：$Bi_m=\dfrac{h_mR}{D_0}$，读法是"**内部扩散阻力 ÷ 外部对流传质阻力**"。
>
> | 范围 | 谁是瓶颈 | 模型可以怎么简化 |
> |---|---|---|
> | $Bi_m\ll1$ | 外部对流传质 | 可近似认为表面含水率恒等于 $C_{air}$（第一类边界条件） |
> | $Bi_m\sim1$ | **两者相当** | **必须同时保留内部扩散与第三类边界条件** |
> | $Bi_m\gg1$ | 内部扩散 | 可近似认为表面立即与空气平衡，整体由内部扩散控制 |
>
> 本题 $Bi_m=2.8\sim3.2$，正好落在中间，**两个阻力谁都不能省**。这一条直接支撑论文中的一句话："由于 $Bi_m$ 为量级 1 的数，表面对流传质与内部扩散同时构成传质阻力，因此必须同时保留内部扩散方程与第三类边界条件。"这也是你回答"能不能只用集总参数模型"这类质疑的依据。
>
> **扩散时间 $\tau_D=R^2/D_0$ 的来历**：纯量纲分析。$D$ 的单位是 m²/s，所以 $R^2/D$ 必然是时间，它代表"扩散走完半径 $R$ 所需的特征时间"。$\tau_D$ 的作用相当于一把尺子：实际烘干时间通常是它的 1~3 倍。本题 $\tau_D\approx19.7$ h、结果 57 h $\approx2.9\tau_D$——在 $Bi_m\sim3$ 且 $D$ 随 $C$ 迅速衰减的情况下，这个倍数完全合理，8.5 节把它列为定性检验之一。
>
> **$\tau_T\ll\tau_D$ 这个不等式是怎么"用"的**：它说明**温度是快过程、含水率是慢过程**。由此可以直接推论：在含水率发生显著变化的整个过程中，温度几乎随时处于"准稳态"，径向温差很小。这条结论在 3.3 节的结果解读里被直接验证（3 h 时径向温差只剩 0.07 ℃）。

### 1.8 论文段落模板

> 由圆柱薄壳微元的守恒分析得到径向一维耦合控制方程组 (1)(2)。表面为第三类边界条件 (3)(4)，轴心为对称条件 (5)，初始场由 (6) 给出。引入 $Bi_m$、$\tau_D$ 等特征数可知……（接上面的解读）。

---

### 1.9 本章小结（可直接作为论文"模型建立"的开头段）

> **到这里，四问共用的"骨架"已经搭完，后面的内容都是往骨架上加东西。** 可以这样概括：
>
> 控制方程 (1)(2) 描述"内部怎么变"，边界条件 (3)(4) 描述"表面怎么和环境交换"，对称条件 (5) 描述"轴心为什么不用管"，初始条件 (6) 描述"一开始是什么状态"。四问的区别只体现在三处：物性系数 $D,\rho,c_p,k$ 取哪套经验公式；边界上的半径取常数 $R_0$ 还是函数 $R(t)$；以及要不要额外求一个"达标时刻"。
>
> **论文写作建议**：把这一段话展开成 2~3 句，就得到了"模型建立"一节的开场白，然后分小节给出式 (1)~(6)。这样读者会先掌握全局，再看细节，理解成本最低。

---

## 第 2 章 问题 1：预热平衡阶段（附录 2，常数物性）

### 2.1 本问特有的物性

由附录 2：

$$\rho=820,\qquad c_p=2600,\qquad k=0.36,\qquad D=7\times10^{-9}e^{-0.89/C}\tag{8}$$

$D$ **只依赖含水率**，与温度无关；$\rho,c_p,k$ 为常数，因此本问是变系数线性问题。

> **$D=7\times10^{-9}e^{-0.89/C}$ 怎么读（这是本问唯一"不平庸"的公式）**：写成 $D=D_\infty e^{-E/C}$ 的形式，属于**类 Arrhenius 经验式**的变体。它的含义是：含水率越低，扩散系数越小。物理直觉是——药材里的水并不都是自由水，大部分被细胞壁与纤维"抓住"，含水率越低，剩下的越是难脱除的结合水，等效扩散阻力就越大，所以 $D$ 越小。
>
> **注意 $C\to0$ 附近的行为**：$e^{-0.89/C}$ 在 $C\to0$ 时以极快的速度趋于 0（超指数衰减）。这带来两个后果：(1) 物理上"最后那一点水几乎不动"，正是干燥后期极慢的原因；(2) 数值上要小心，**不要**在 $C=0$ 附近取对 $C$ 极敏感的步长，否则会出现 $D$ 的剧烈跳变。本问题中 $C$ 始终大于 0.15，不存在这个风险，但论文里提到"经验公式适用范围"时值得写一句。
>
> **为什么本问可以算作"线性问题"**：$\rho,c_p,k$ 全是题目给的常数，$D$ 只依赖 $C$。虽然系数依赖未知量、严格说仍是拟线性，但**没有形成"温度 ↔ 水分"的双向耦合**——温度影响不了水分（$D$ 不含 $T$），水分只能单向通过 $\rho c_p(C)$ 影响温度。因此本问的耦合是**单向**的，问题 2 之后才是**双向**的。论文里可以写成"情形一：单向耦合；情形二、三：双向耦合"，这是一个很有说服力的层次划分。

### 2.2 烘房环境条件的处理

附件 1 给出 $0\sim14400$ s、每 60 s 一个点的 $T_{air},C_{air}$。本问只算到 1800 s，直接落在数据区间内，采用**分段线性插值**

$$T_{air}(t)=T_i+(T_{i+1}-T_i)\frac{t-t_i}{\Delta t},\qquad t_i=60i,\ \Delta t=60\ \mathrm{s}\tag{9}$$

单位换算：$T_{air}[\mathrm{K}]=T_{air}[^\circ\mathrm{C}]+273.15$。

> 问题 2、3、4 算到几十小时，需要**外推**：超过 14400 s 后取最后一个数据点（常数外推）。理由是烘房 4 h 后已进入恒温干燥阶段，$T_{air}\to50.16$ ℃、$C_{air}\to0.0499$ kg/kg 基本不变。论文中要写明这条外推假设。

> **什么是分段线性插值**：附件 1 只在离散时刻给了烘房温度与湿度，而方程在整个时间区间上都要用到它们，因此必须构造一个"处处有定义、且尽量贴近原始数据"的函数。分段线性插值就是**把相邻两个数据点用直线连起来**。它的优点是简单、不会破坏数据的单调性、也不会像高次多项式插值那样在端点附近乱甩（龙格现象）。论文里写"采用分段线性插值构造 $T_{air}(t)$ 与 $C_{air}(t)$"即可。
>
> **区间内插值 vs 区间外外推，是性质完全不同的两件事**：
> - **插值**（$t\le14400$ s）：两侧都有数据支撑，误差是 $O(\Delta t^2)$，可以放心用。
> - **外推**（$t>14400$ s）：**没有任何数据支撑**，只能靠物理假设。本手册取"最后一点取常数"，依据是恒温干燥阶段烘房参数已被主动控制在恒定值附近（14400 s 时 $T_{air}=50.165$ ℃、$C_{air}=0.04986$ kg/kg，且此前数小时已基本平稳）。
>
> **这条外推假设必须写进论文**，而且要写成假设、给出理由。这是整篇论文里最容易被审阅人打问号的地方——题目只给到 4 h，而你算了 50 多个小时。
>
> **对应到 COMSOL 里的做法**：把两条曲线各做成一个插值函数（Interpolation），并把**外推方式设为"常数（Constant）"而不是"线性外推（Linear extrapolation）"**。线性外推会让烘房温度继续无限升高，是明显的物理错误。这就是你在 COMSOL 里建 `TairC`、`Cair` 两个插值函数时那个选项的意义。

### 2.3 本问定解问题

式 (1)(2) 中代入 (8)，配 (3)(4)(5)(6) 与 (9) 即得完整模型。

> **"有限体积法 + 全隐式时间推进"这句话在回答什么问题**：它在回答"你是怎么把连续方程变成可以上机计算的代数方程的"。前半句说的是**空间**离散方式，后半句说的是**时间**离散方式。论文里出现这个短语时，一定要在第 6 章（或附录）给出三个具体参数——**离散格式、网格数、时间步长**，否则审阅人无法判断你的数值精度。细节见第 6 章。
>
> **为什么网格取 $N=400$、时间步取 $\Delta t=1$ s**：不是拍脑袋定的，判据是"再加密一半，结果几乎不变"。这正是 8.3 节收敛性检验要做的事。论文里最好把这句话显式写出来："经网格与时间步收敛性检验，$N=400$、$\Delta t=1$ s 时结果已收敛（进一步加密的相对变化小于 0.1%）。"**有这句话，"数值结果"才从"一次计算"变成"一个可信的解"。**

### 2.4 求解与结果

有限体积法 + 全隐式时间推进（细节见第 6 章），$N=400$ 个控制容积、$\Delta t=1$ s。1800 s 结果：

| 位置 | 温度 | 含水率 |
|---|---|---|
| 中心 $r=0$ | 33.5263 ℃ | 2.5500 |
| 表面 $r=R$ | 36.7598 ℃ | 1.5088 |

物理解读（可直接写进论文）：

1. **温度**：表面先热，$Bi=1.39$ 说明外部对流与内部导热阻力相当，形成外高内低的径向温差；到 1800 s 时中心与表面仍差 3.2 ℃，说明 30 min 内还没有完全进入准稳态。
2. **含水率**：只有表层明显下降（$r\ge0.9$ cm 才开始变化，中心仍是 2.5500）。用扩散长度估计
   $$\ell\sim\sqrt{D_0t}=\sqrt{4.94\times10^{-9}\times1800}=3.0\times10^{-3}\ \mathrm{m}=0.3\ \mathrm{cm}$$
   与表中只有最外两三列变化完全一致。
3. **表面平衡**：表面含水率 1.5088 是被对流传质"吹低"的表面值，验证其通量 $h_m(C_s-C_{air})=8\times10^{-7}\times(1.5088-0.0331)=1.18\times10^{-6}$ 与内部扩散通量同量级，符合 $Bi_m=3.2$。

> **这里用的 $C_{air}=0.0331$ 是 $t=1800$ s 时刻的烘房湿度**（由附件 1 分段线性插值得到），不是初始时刻的 0.01963。**凡是在正文里代入环境量的地方，都要先确认"取的是哪个时刻的值"**——这是本题最容易出错的一类细节，因为烘房温湿度在前 4 h 一直在变。
>
> **"表面平衡"这条检验的意义**：它验证的是边界条件的**自洽性**——表面通量按 (4) 计算，应当与内部的扩散通量大小相当（因为表面不存水，进多少出多少）。如果两者差好几个数量级，就说明 $h_m$ 或 $D$ 的取值有问题。这种"在同一时刻比较两边通量"的做法，是检验边界条件实现是否正确的最直接手段。

> **扩散长度 $\ell\sim\sqrt{D_0t}$ 是怎么来的（一个非常常用的估算工具）**：来自"量纲配平"。扩散方程 (1) 里 $\dfrac{\partial C}{\partial t}\sim D\dfrac{\partial^2C}{\partial r^2}$，把导数换成"变化量 ÷ 特征尺度"得 $\dfrac{\Delta C}{t}\sim D\dfrac{\Delta C}{\ell^2}$，约去 $\Delta C$ 立刻得到 $\ell\sim\sqrt{Dt}$。它的含义是：**在时间 $t$ 内，只有表面附近大约 $\ell$ 厚的一层会发生明显变化，更里面几乎感受不到。** 这就是"扩散穿透深度"。
>
> **用它要小心的地方**：$\sqrt{D_0t}$ 用的是**初始**扩散系数 $D_0$。当含水率下降、$D$ 变小之后，实际穿透深度会明显小于这个估计。因此这个公式只能用来做**量级判断**（$\ell\approx0.3$ cm 与"只有最外两三列在变"完全一致），**不能**反过来用它算精确结果。
>
> **温度剖面为什么是"外高内低"，含水率为什么是"内高外低"**：边界条件 (3) 在 $T_{air}>T$ 时把热量送进药材，靠近表面的材料先升温，热量再靠导热往中心传，于是 $T$ 沿半径**递减**；反过来，水分从表面被空气带走，表面先失水，内部的水再往外补，于是 $C$ 沿半径**递增**。**记住这两条方向——它们就是 8.5 节定性检验里的两条判据，也是你一眼判断仿真结果有没有搞反的快捷方法。**

---

## 第 3 章 问题 2：整个烘干过程（附录 3，变物性）

### 3.1 本问特有的物性

附录 3（问题 2、3 统一使用）：

$$\rho=650+128C,\qquad c_p=1450+2736\frac{C}{C+1},\qquad k=0.21+0.38\frac{C}{C+1}\tag{10}$$

$$D=2.4\times10^{-3}e^{-0.45/C}e^{-3850/T}\tag{11}$$

式 (11) 中 $T$ 必须用**开尔文**。此时 $D$ 同时依赖 $C$ 与 $T$，方程 (1)(2) 通过 $C$ 完全双向耦合：含水率下降 → 物性改变 → 温度分布改变 → $D$ 改变 → 反过来又影响含水率。

> **附录 3 的四个公式分别长什么样、为什么是这个形状**：
>
> | 公式 | 形状 | 物理含义 |
> |---|---|---|
> | $\rho=650+128C$ | 线性 | 水越多整体越重，含水量每增加 1 kg/kg，表观密度增加 128 kg/m³ |
> | $c_p=1450+2736\dfrac{C}{C+1}$ | 饱和型 | 水的比热比干物质大，含水率越高越接近水的量级；$\frac{C}{C+1}$ 是"水占的质量分数"，天然落在 0~1 之间 |
> | $k=0.21+0.38\dfrac{C}{C+1}$ | 饱和型 | 水比干物质更"能导热"，含水率越高导热越好 |
> | $D=2.4\times10^{-3}e^{-0.45/C}e^{-3850/T}$ | **两个指数的乘积** | 前者是**含水率效应**，后者是 **Arrhenius 温度效应** |
>
> **$e^{-3850/T}$ 是什么**：标准 Arrhenius 形式 $e^{-E_a/(RT)}$ 的简化写法，常数 3850 已经把 $E_a/R$ 合并进去了。它的含义是"温度越高，分子与水分的活动越剧烈，扩散越快"。$T$ 从 301 K（28 ℃）升到 323 K（50 ℃）时，$e^{-3850/T}$ 从 $2.80\times10^{-6}$ 升到 $6.7\times10^{-6}$，**约 2.4 倍**——温度对扩散的加速相当显著。
>
> **$T$ 必须用开尔文，这一条没有例外**：如果误填成摄氏度数值（例如 50），$e^{-3850/50}=e^{-77}$ 会直接下溢为 0，扩散系数变成 0，药材就永远干不了。这类错误在 COMSOL 里**不会报错**，只会给你一个"看起来跑完了"的错误结果，所以必须靠单位检查（8.5 节定性检验）来发现。
>
> **双指数结构的物理后果**：干燥前期 $C$ 大、$T$ 高 → $D$ 大 → 湿分快速外迁；后期 $C$ 小 → $e^{-0.45/C}$ 骤减 → 干燥被"锁死"。所以烘干曲线**必然是"先快后慢"**、末段非常平缓。这一条与题目"烘干过程一般持续 2-3 天"的先验判断完全一致，可以互相印证。

### 3.2 "预热平衡 + 恒温干燥"如何体现在模型里

模型不需要人为分段：两个阶段的差异**完全由附件 1 的 $T_{air}(t),C_{air}(t)$ 体现**。前 4 h 烘房温度由 28 ℃ 升到 50 ℃、湿含量由 0.0196 升到 0.05，之后保持恒定；边界条件 (3)(4) 自动把这种变化传入药材内部。

> **为什么"两个阶段的差别完全由边界条件体现"**：模型的**内部方程从头到尾没变**，变的是外界给它的"命令"——前 4 h 烘房在升温升湿，之后基本恒定。这体现了一个很重要的建模思想：**同一套方程，改变边界条件就能描述不同阶段，不需要换模型、更不需要人为把时间切开。**
>
> **为什么不该为两个阶段写两套子模型**：因为两者之间没有物理突变（温度、湿度都是连续变化的）。强行分段反而引入两个新麻烦：在哪个时刻切换？切换处解是否连续？用连续函数描述环境、一套方程算到底，是更干净也更稳的做法。论文里可以这样写："本文模型不引入分段假设，预热平衡阶段与恒温干燥阶段的转变完全由附件 1 给出的边界条件 $T_{air}(t),C_{air}(t)$ 自动刻画。"

### 3.3 结果与解读

表 3、表 4（result2.xlsx 抽出）显示：

1. **温度**：0.5 h 中心 32.16 ℃，3 h 中心 49.78 ℃、表面 49.86 ℃，径向温差从 3.23 ℃ 收缩到 0.07 ℃——温度已接近均匀（与 $\tau_T\approx1$ h 一致），此后是**准等温干燥**。
2. **含水率**：3 h 时中心从 2.55 只降到 1.77，表面已降到 1.01，呈外干内湿的陡峭剖面。原因是 $D\propto e^{-0.45/C}$ 随含水率下降急剧减小，形成"表皮硬化"效应。
3. **与问题 1 的对比**：同样是 1800 s，问题 1 的表面含水率是 1.5088，问题 2 是 1.6475。差异来自附录 2 与附录 3 的物性公式不同，而不是建模方法不同——这正是题目要求"问题 2、3 统一采用附录 3"的原因。

> **"表皮硬化"（case hardening）现象——本问最值得写进论文的物理机制**：由于 $D\propto e^{-0.45/C}$，表面含水率一旦开始下降，表层的 $D$ 也随之骤降，水分被"堵"在里面出不来，于是内部含水率的下降比按常数 $D$ 估计的慢得多。这就是为什么 3 h 时表面已降到 1.01、而中心还有 1.77。论文措辞建议："由于扩散系数随含水率指数衰减，干燥过程中会自发形成低渗表层，产生阻塞效应，这也是干燥后期速率急剧下降的机理之一。"
>
> **陡峭剖面对数值方法意味着什么**：陡的梯度会放大离散误差。这解释了为什么问题 3、4 的网格误差（0.2~0.4%）比想象中大，也是 8.3 节必须做网格收敛检验的原因。论文的误差分析里可以补一句："解在表面附近梯度较大，离散误差主要由该区域贡献。"

---

## 第 4 章 问题 3：烘干所需时间的确定

### 4.1 把"各处低于 0.15"写成数学条件

记 $M(t)=\max\limits_{0\le r\le R}C(r,t)$，则

$$t_{dry}=\min\{t>0:\ M(t)<0.15\}\tag{12}$$

> **这个定义的三个细节，写论文时都要交代清楚**：(1) $M(t)=\max_r C(r,t)$ 是**整个药材上的最大含水率**，而不是中心值——因为题目要求的是"药材各处都低于 0.15"；(2) 要求的是**严格低于**，所以写成开区间 $M(t)<0.15$；(3) 取的是满足该条件的**最小时间**，也就是"第一次达标"的时刻，而不是"最后一次达标"。把这三句话说完整，定义就没有任何歧义了。
>
> **为什么不用"解方程 $C=0.15$"的写法**：因为 $C(0,t)-0.15=0$ 是一个没有解析表达式的方程，只能数值求根。写成 $\min\{t:\cdots\}$ 的好处是它与"时间推进 + 每步判断"这种数值做法天然对应，实现上不会增加任何代价。
>
> **一个容易踩的坑**：如果只检查中心点而不说明为什么可以这样简化，审阅人会质疑"万一靠近表面的某点反而更晚达标呢？"——这正是下一节 4.2 要回答的问题。**先提出问题、再解答**，是很适合写进论文的叙事结构。

### 4.2 为什么可以只看中心

表面持续向空气排湿（当 $C(R,t)>C_{air}$ 时边界条件 (4) 为出流），轴心是零通量面，因此含水率沿半径**单调不减**：$C(0,t)\le C(r,t)\le C(R,t)$，于是 $M(t)=C(0,t)$，

$$t_{dry}=\min\{t:\ C(0,t)<0.15\}
=\min\left\{t:\ \max_{r}C(r,t)<0.15\right\}$$

补充论证（论文可写）：若某点 $r_0$ 出现局部极小，由抛物型方程的极值原理，在该点 $\partial C/\partial t\ge0$，极小值不会继续下降，故不可能比中心先达标；用中心判据是充分且必要的。

> **极值原理（本节用到的最重要的一个定理）**：对抛物型方程 $\partial_t u=\mathcal{L}u$（$\mathcal{L}$ 是扩散型算子），如果初始值与边界值都不超过 $M$，那么**在整个时空区域上都有 $u\le M$**；对最小值也有相应结论。直观理解是：扩散只会"把高低抹平"，绝不会在内部凭空制造出一个比周围都极端的值。论文里如果要用它，规范的表述是"由抛物型方程的极值原理"。
>
> **"含水率沿半径单调不减"这一步严格怎么证**：轴心是零通量面、表面持续往外排湿，水分只能从内往外走。严格的写法是反证——设 $C$ 沿 $r$ 单调不减这一性质在某时刻首次被破坏，那么在该时刻必有内部极值出现；而由极值原理，内部极值既不能自发产生也不能自发消失，矛盾。因此 $C(0,t)\le C(r,t)\le C(R,t)$，即**中心最湿、表面最干**。
>
> **这条结论的实际价值**：它把"要给 5 个位置分别设判据"降成"只看中心一个数"，既节省算力，也让论文里的判据写得干净利落：$t_{dry}=\min\{t:C(0,t)<0.15\}$。**这是一处"用数学定理省掉一整个数值搜索"的漂亮结论，建议在论文里单独成段。**
>
> **补充一句严谨性说明**：在物理上这条结论有一个前提——边界始终是"出流"（即 $C(R,t)>C_{air}(t)$）。本题烘房湿度始终远低于表面含水率，该前提成立；论文里可以加一句"在本问题的参数范围内，表面始终为水分流出边界，故上述单调性成立"。

### 4.3 数值求 $t_{dry}$ 的算法

$C(0,t)$ 关于 $t$ 单调下降，$f(t)=C(0,t)-0.15$ 只有一个零点。

| 方法 | 做法 | 精度/成本 |
|---|---|---|
| A 时步扫描（本工作采用） | 时间推进中每步判断 $C_0^{n}<0.15$，首次满足即记录 $t^n$ | 误差 $\le\Delta t$；$\Delta t=10$ s 时相对误差 $<0.02\%$；成本几乎为零 |
| B 二分法 | 对 $f(t)$ 在 $[t_1,t_2]$（未达标 / 已达标）上二分，$t_{k+1}=(t_k^{lo}+t_k^{hi})/2$ | 20 次迭代精度 $10^{-6}$；但要重算 20 次瞬态问题，成本高 |

> **两种算法怎么选**：方案 A 是"边算边看"，几乎不增加任何成本；方案 B 是"先算一遍粗解、再回头精确定位"。因为全隐式格式允许取很小的固定时间步（10 s，相对误差 $<0.02\%$），方案 A 的精度完全够用，**这就是本文选 A 的理由**。论文里给出这个对比表的好处是：审阅人会看到你意识到"还存在一个更贵但更精确的方法"，并且**有理由地**选择了便宜的那个——这比"我只会这一种"强得多。
>
> **"零点唯一"这个条件从哪来**：$C(0,t)$ 随时间单调下降（水分只能往外走），所以 $f(t)=C(0,t)-0.15$ 是单调函数，而**单调函数的零点至多一个**。这个"至多一个"正是二分法能用的前提：如果存在多个零点，二分法可能收敛到你并不想要的那一个。论文里顺手提一句"$f(t)$ 单调，故零点唯一"，会显得非常严谨。

### 4.4 结果与交叉验证

| 数据来源 | 烘干时间 |
|---|---|
| COMSOL 有限元模型（result3.xlsx） | **57.15 h** |
| 独立有限体积程序（$N=400,\ \Delta t=10$ s） | 57.02 h |
| 上述结果 Richardson 外推（网格步长趋于 0） | ≈57.18 h |

三者相对偏差 $<0.25\%$，且落在题目提示的"2~3 天"区间（57.15 h ≈ 2.38 天）。表 5 的"烘干结束时间"行建议填 **57.2 h**。

> **"交叉验证"和"重复计算"根本不是一回事**：把同一个程序用同一套参数跑两遍，叫重复计算，什么也验证不了——程序里的系统性错误会被原封不动地复制一遍。真正的交叉验证要求**两个解的实现路径不同**：这里是 COMSOL 的**有限元**（弱形式、二次单元、自适应时间步）对上手写程序的**有限体积**（守恒积分、一阶网格、固定时间步）。两条完全不同的路只有在模型、参数、边界条件都正确时才会收敛到同一个答案；任意一处写错，两者会明显分家。
>
> **"三者相对偏差 <0.25%"这句话该怎么写得可信**：不要只说"吻合良好"，要给出**判据**和**数值**。建议写成："三种独立途径（COMSOL 有限元、有限体积独立复算、Richardson 外推极限）给出的烘干时间分别为 57.15 h、57.02 h 与 57.18 h，两两相对偏差不超过 0.25%，均落在题目提示的 2~3 天范围内。这说明该结果是模型的收敛值，而非某一次计算的偶然产物。"
>
> **表 5 的"烘干结束时间"为什么建议填 57.2 h 而不是 57.1 h**：题目要的是"药材烘干所需要的时间"，而工艺上应当取**保守可行**的值——即按其执行一定能达标。57.2 h 略大于计算值 57.15 h，比取 57 h 更稳妥。论文里注明"计算值 57.15 h，按工艺保守取 57.2 h"即可，**不要**悄悄取整而不解释。
>
> **0.23% 的差异该归到哪一类误差**：它落在你自己估计的离散误差量级之内（8.3 节证明有限体积解随网格一阶收敛，$N=400$ 时相对误差约 0.2%），因此属于**数值误差**而非模型分歧。这个判断很重要——它决定了这句话应该写在论文的"数值误差"段落，而不是"模型不确定性"段落。搞错归类会让误差分析自相矛盾。

---

## 第 5 章 问题 4：考虑尺寸收缩的烘干模型

### 5.1 收缩数据的处理

附件 2 给出 $0\sim259200$ s、每 1800 s 一个点的半径（单位 cm）：

$$R(0)=2.000\ \mathrm{cm}=0.02000\ \mathrm{m}
\;\longrightarrow\;
R(259200)=1.198\ \mathrm{cm}=0.01198\ \mathrm{m}$$

用分段线性插值构造连续函数 $R(t)$（代码里 $Rcm(t)$ 为插值函数，$R_t=Rcm(t)\times0.01[\mathrm{m}]$）。

> **为什么"半径在变"会让问题难一大截**：前三个问题里计算区域 $[0,R_0]$ 是固定的，网格画一次就能一直用。问题 4 里边界在动，网格必须跟着动，否则**节点会和材料脱钩**——你今天在这个节点上算的材料，明天可能已经跑到药材外面去了。这类问题在数学上叫**动边界问题 / 移动区域问题**，它是问题 4 比问题 3 本质更难的原因，也是本问真正的考点。
>
> **半径数据的用法要理解准确**：附件 2 给的是"每个时刻药材的**半径**"，也就是只描述了**表面**在怎么运动。药材内部每一个材料点怎么动，题目并没有给。所以必须自己补一个运动学假设（即 5.3.1 的均匀收缩假设）。**这是问题 4 里最关键、也最需要说明理由的一个建模判断。**
>
> **顺手注意 1.198 cm 这个数**：它比原始半径 2 cm 小了约 40%（体积收缩到原来的约 36%）。这不是小扰动，因此"到底要不要考虑收缩"必须专门做一次对比实验（即下面 5.2 的路线 A），不能靠嘴说"影响不大"。

### 5.2 三条可选路线（论文里说明为什么选第二条）

| 路线 | 做法 | 结果 | 是否可用 |
|---|---|---|---|
| A 忽略收缩 | 仍用 $R_0=2$ cm，只把物性换成附录 4 | 中心含水率 72 h 仍有 0.2228，**永远降不到 0.15** | ✗ 与题目"尺寸变化"矛盾 |
| **B 材料坐标（拉格朗日）均匀收缩** | 把方程写到材料坐标 $X$ 上，$r=X R(t)/R_0$ | 烘干时间 **50.7 h** | ✓ **推荐** |
| C 归一化坐标 $y=X/R_0$ | 与 B 代数等价（见 5.3.4） | 与 B 相同 | ✓ 可作独立复算 |

路线 A 的作用很重要：它说明**收缩不是可以忽略的细节，而是决定能否达标的关键因素**（这也是问题 4 与问题 3 的本质差别），论文里应写出这个对比。

> **路线 A 为什么"永远降不到 0.15"（这不是程序出错）**：$D$ 随 $C$ 下降得极快（$e^{-0.3/C}$），当 $C$ 接近 0.15 时 $D$ 已经小到"水分几乎不动"；与此同时表面还在持续向空气排湿、把表层含水率压向 $C_{air}$。结果是中心含水率以极慢的速度逼近某个值。**物理上这意味着：如果药材不收缩、水分必须自己走完 2 cm 的全程，那么按题目给定的物性，它在 72 h 内达不到 0.15。**
>
> **这段"反例"在论文里的价值很高**：它把"收缩"从"锦上添花的细节"提升为"决定结论的关键因素"。推荐写法："若不考虑尺寸变化（路线 A），中心含水率在 72 h 时仍为 0.2228，无法满足题目给出的烘干要求；只有计入收缩（路线 B）才能在约 50.7 h 内达标。这说明尺寸变化是问题 4 的核心机理，而非可忽略的高阶效应。"
>
> **同时要诚实交代局限**：路线 A 用的是"半径固定、只换成附录 4 物性"的组合，本质上是一种**对照情景**，并不是题目要求的情形。论文里把它定位成"对照实验"即可，**不要**写成"题目要求的结果"，否则会被认为审题有误。

### 5.3 路线 B 的推导

#### 5.3.1 收缩运动学（均匀收缩假设）

设时刻 $t$、初始位置为 $X\in[0,R_0]$ 的材料点位于半径

$$r(X,t)=X\,\frac{R(t)}{R_0}\tag{13}$$

即所有材料按同一比例向轴心收缩（表面材料点 $X=R_0$ 始终位于表面 $r=R(t)$）。这是干燥文献中最常用的**均匀（同比例）收缩假设**；在 COMSOL 里用"变形域 + 边界位移"的平滑方程求出的位移场在 1D 下正好是线性的，与此完全一致。

> **什么是"材料坐标"（拉格朗日视角）**：欧拉视角是"盯着空间中固定的位置看"（$r$ 固定，问这个位置的水分如何变化）；拉格朗日视角是"盯着同一块材料看"（$X$ 固定，问这块材料的水分如何变化）。$X$ 就是每个材料点**在初始时刻**到轴心的距离，称为材料坐标或参考坐标。它一旦被贴上就跟着材料一起运动，永远不变。
>
> **为什么问题 4 非用它不可**：因为在材料坐标下，计算区域永远是 $[0,R_0]$，**固化不动**；边界收缩带来的全部效应被吸收进了方程系数（那个 $(R_0/R)^2$ 因子）和边界条件里。这样就完全不需要真的去移动网格，程序结构与前三问完全一样。**这是把动边界问题化为定边界问题的一个标准技巧**，值得在论文里单独点出来。
>
> **把映射关系写全并自检两个端点**：$r(X,t)=X\dfrac{R(t)}{R_0}$，反解得 $X=\dfrac{R_0}{R(t)}r$。检查端点：$X=0$ 时 $r=0$（轴心不动，合理）；$X=R_0$ 时 $r=R(t)$（表面材料点始终位于表面，合理）。**两端都自洽，说明这个映射没有把材料"漏掉"或"重叠"。** 论文里给出这两行自检会很有说服力。
>
> **和 COMSOL 的对应关系要理解到位**：COMSOL 的"变形几何"本质上就是在内部维持一套材料坐标——你把表面位移指定为 $R_t-R_{drug}$，它就按平滑规则把内部材料点挪到相应位置。所以 5.6 节说"用变形几何 = 用材料坐标模型"，**这不是比喻，而是二者数学上等价**。理解这一点，你才能解释"为什么仿真结果和独立解只差 3%"。

#### 5.3.2 质量守恒（以干物质为基准）

取材料壳层 $[X_1,X_2]$（干物质质量恒定）。当前时刻该壳层的体积为
$$\mathrm{d}V=\pi\left(X_2^2-X_1^2\right)\left(\frac{R}{R_0}\right)^2\quad(\text{单位长度})$$

半径为 $r$ 的面上，通量（水分/时间）为

$$F=-D\frac{\partial C}{\partial r}\times 2\pi r
=-D\frac{R_0}{R}\frac{\partial C}{\partial X}\times 2\pi X\frac{R}{R_0}
=-2\pi X D\frac{\partial C}{\partial X}\tag{14}$$

注意 $\dfrac{R}{R_0}$ 因子正好**抵消**——这是材料坐标的方便之处。

由"水分变化 = 流入 − 流出"，并把干物质质量（$\rho_d 2\pi X (R/R_0)^2\mathrm{d}X$，其中 $\rho_d$ 与 $X$ 无关）约去，得到

$$\boxed{\;\frac{\partial C}{\partial t}
=\left(\frac{R_0}{R(t)}\right)^{2}\frac{1}{X}\frac{\partial}{\partial X}
\left(X D(C,T)\frac{\partial C}{\partial X}\right)\;}\tag{15}$$

> **为什么选"以干物质为基准"来列守恒**：因为**干物质不参与迁移**——它既不生成也不消失，只是随着水分流失让体积缩小。因此"某一块干物质所占据的体积"是一个随时间变化、但**质量恒定**的标记物，拿它当记账单位最稳妥。如果改用"空间中固定的体积"，每一时刻都会有干物质穿过这个体积的表面，守恒式里就要多出对流项，白白增加复杂度。
>
> **式 (14) 里那个漂亮的抵消是怎么回事**：通量写作 $-D\dfrac{\partial C}{\partial r}\times2\pi r$。换到材料坐标需要两件事——$\dfrac{\partial C}{\partial r}=\dfrac{R_0}{R}\dfrac{\partial C}{\partial X}$，以及 $2\pi r=2\pi X\dfrac{R}{R_0}$。前者带来一个 $R_0/R$、后者带来一个 $R/R_0$，**恰好互为倒数，于是整体上去掉了**。这不是巧合，而是反映了一个一般规律：几何缩放对"梯度"和"面积"的影响方向相反。
>
> **写作提示**：论文里最好把 (14) 的这两步缩放**显式写出来**（就像上面这样）。这是问题 4 推导里唯一"看起来像巧妙约分"的地方，写出来审阅人才会相信你确实推过，而不是从文献里抄来的结论。

#### 5.3.3 边界条件与初始条件

表面通量 $2\pi R\,h_m\left(C(R,t)-C_{air}\right)$ 与式 (14) 在 $X=R_0$ 处相等：

$$-2\pi R_0 D\frac{\partial C}{\partial X}\Big|_{X=R_0}=2\pi R\,h_m\left(C-C_{air}\right)
\;\Longrightarrow\;
\boxed{\;-D\frac{\partial C}{\partial X}\Big|_{X=R_0}=\frac{R}{R_0}h_m\left(C-C_{air}\right)\;}\tag{16}$$

温度方程同理：

$$\rho c_p\frac{\partial T}{\partial t}
=\left(\frac{R_0}{R}\right)^{2}\frac{1}{X}\frac{\partial}{\partial X}
\left(X k\frac{\partial T}{\partial X}\right),\qquad
-k\frac{\partial T}{\partial X}\Big|_{X=R_0}=\frac{R}{R_0}h\left(T-T_{air}\right)\tag{17}$$

轴心仍是零通量，初始条件仍为 $T_0,C_0$。

> **边界条件 (16) 里多出来的 $\dfrac{R(t)}{R_0}$ 是什么**：左边是**材料坐标下的通量**（由 $\partial C/\partial X$ 算出），右边是**真实表面积上的通量**。表面当前的真实半径是 $R(t)$ 而不是 $R_0$，所以要乘上 $R/R_0$，把"按材料坐标算出的通量"换算成"按真实面积算出的通量"。**漏掉这个因子，问题 4 的结果就会错**——而且 8.4 节的积分守恒检验对这一点极其敏感（残差会从 0.5% 跳到几十个百分点）。
>
> **初始条件为什么与前三问完全相同**：$t=0$ 时 $R(0)=R_0$，材料坐标与空间坐标重合，所以 $T(X,0)=T_0$、$C(X,0)=C_0$ 与前三问一致。这提示一个重要认识：**问题 4 与前几问的差别只体现在"系数"和"边界"上，初始条件用的是同一套**。抓住这一点，你在论文里的模型叙述就不会重复啰嗦。

#### 5.3.4 等价形式（用于交叉验证）

令 $y=X/R_0\in[0,1]$，代入 (15)(16) 得

$$\frac{\partial C}{\partial t}
=\frac{1}{yR(t)^{2}}\frac{\partial}{\partial y}
\left(yD\frac{\partial C}{\partial y}\right),\qquad
-D\frac{\partial C}{\partial y}\Big|_{y=1}=R(t)h_m\left(C-C_{air}\right)\tag{18}$$

(18) 与 (15) 只是自变量缩放，数值结果应当完全相同——可以用它做**双重实现互验**（论文中可写成"本文用两种等价坐标分别计算，结果一致"）。

> **"两种等价坐标互验"属于哪一类验证**：它是一次**自洽性检验**——同一个物理模型用两种写法实现，结果必须一致。如果两者不一致，说明至少有一次的变量代换算错了（例如漏掉某个雅可比因子）。**它比"把同样的代码跑两遍"强得多，因为它检验的是推导，而不是实现。**
>
> **使用时的注意事项**：两种写法在**连续层面**严格等价，但在**离散层面并不等价**——网格画在不同的坐标系上，离散误差不同，所以两者应当"接近，但不必完全相等"（差别应落在离散误差的量级内）。论文里表述为"两种等价坐标下的计算结果相互一致（差异小于 X%，处于离散误差量级）"，**不要把"完全相等"设成要求**，否则会给自己制造一个假的失败判据。

#### 5.3.5 物理意义（写论文时重点解释）

1. **扩散项被放大**：等效扩散系数 $D_{\text{eff}}=D\left(R_0/R\right)^2$，当 $R$ 由 2.0 cm 降到 1.2 cm 时放大 $2.8$ 倍，即"路程变短、梯度变陡"。
2. **单位干物质的表面积变大**：表面通量系数由 $h_m$ 变为 $\dfrac{R_0^2}{R}h_m$，等价于"药材越小，相对表面积越大，越容易干"。
3. 两个效应叠加，使 50.7 h 达标；若忽略收缩则 72 h 都无法达标（路线 A）。这从物理和数值两方面说明**收缩不可忽略**。

> **这三点怎么变成论文里的"机理分析"**：把 1、2 两条合起来说——"收缩使等效扩散系数放大 $(R_0/R)^2$ 倍（由 1 变为 2.78），同时使单位干物质的表面积增大 $R_0^2/R$ 倍；两个效应方向相同、彼此叠加，因此收缩显著加快干燥。"再加一句 3 作为数值佐证："这一机理的重要性可由对照实验验证：忽略收缩时 72 h 仍未达标，考虑收缩后 50.7 h 即达标。"
>
> **注意区分"机理"和"结果"**：机理是"为什么快"，结果是"快了多少"。论文里先讲机理（系数放大与表面积增大），再给数字（2.78 倍、50.7 h），逻辑最顺。只给数字不讲机理，会被认为只是"跑了个仿真"。

### 5.4 求解

与问题 2、3 用同一套有限体积格式，只是：

- 网格建在**材料坐标 $X$** 上（$N=400$ 个控制容积，$\Delta X=R_0/N$）；
- 每个时间步先由 $R(t)$ 算出缩放因子 $\left(R_0/R\right)^2$ 与边界系数 $2\pi R_0^2 h_m/R$；
- 物性 $k,\rho,c_p,D$ 乘上缩放因子后组装三对角矩阵（细节见 6.1~6.4）。

> **有限体积格式在问题 4 里究竟改了哪两处**：只改了两处——(1) 扩散项乘上缩放因子 $(R_0/R)^2$；(2) 表面通量的系数从 $h_m$ 换成 $\dfrac{R}{R_0}h_m$（写成 $\dfrac{R_0^2}{R}h_m$ 是把面积按 $R_0^2$ 归一化后的等价写法）。**方程的形式、三对角结构、追赶法全都没变。** 这正是材料坐标最大的好处：代码复用率极高，出错的概率也低。
>
> **为什么每个时间步都要重算这两个因子**：因为 $R(t)$ 在变。$R$ 从 2 cm 降到 1.2 cm 的过程中，$(R_0/R)^2$ 从 1 变到 2.78，它是时间的函数而不是常数，不能在建模型时算一次就完事。在 COMSOL 里对应的就是把边界位移写成时间的函数（或插值函数），让变形几何随时间变化——这也是你的模型里"变形几何"必须与"研究/时间"绑定的原因。

### 5.5 结果与验证

**全局指标**

| 量 | 独立有限体积解 | COMSOL（result4.xlsx） | 相对差 |
|---|---|---|---|
| 烘干时间（中心 $C$ 首次 $<0.15$） | 50.71 h（$N=400$） | **52.43 h** | 3.3% |
| 72 h 中心含水率 | 0.1230 | 0.1247 | 1.4% |
| 72 h 表面含水率 | 0.0511 | 0.0512 | 0.2% |

**6 h 步长逐点对比（中心含水率）**

| 时间 | 6 h | 12 h | 18 h | 24 h | 36 h | 48 h | 54 h | 72 h |
|---|---|---|---|---|---|---|---|---|
| 收缩参考解 | 1.7127 | 0.7308 | 0.4046 | 0.2825 | 0.1914 | 0.1551 | 0.1441 | 0.1230 |
| COMSOL | 1.9508 | 0.8707 | 0.4571 | 0.3058 | 0.1993 | 0.1591 | 0.1471 | 0.1247 |
| 相对差 | 12% | 16% | 12% | 8% | 4% | 2.6% | 2.1% | 1.4% |

两者在**后期（>36 h）吻合得很好（1~4%）**，中前期相差 8~16%，烘干时间差 3.3%。这个分布说明两套模型对"最终能不能干、什么时候干"的判断一致，差别主要在前期——原因有三：

1. **收缩运动学的细节**：本文假设同比例收缩（材料点到轴心的相对位置不变），COMSOL 用"变形域"平滑方程 + 边界位移求解；两者在 1D 下虽然都近似线性，但对内部网格位移的规定方式不同；
2. **变形后坐标系中物性的取值方式**：$\rho c_p$ 是按当前体积还是按材料体积计算，会影响温度对湿分扩散的反馈强度，前期温度变化剧烈时影响最大；
3. **有限元与有限体积的离散差异**（前期扩散锋面极陡，该处误差被放大）。

论文里建议这样写：*两套独立实现的烘干时间相差 3.3%（52.43 h 与 50.71 h），均给出"约 2.2 天"的结论；差异主要出现在干燥前期，与收缩运动学假设和变形坐标系下物性处理方式有关，属于模型层面的不确定度。*

> **这张 6 h 步长对比表是整份手册里最需要"讲清楚"的地方**：两套模型对"什么时候能干"的判断完全一致（50.7 h 与 52.4 h，都对应约 2.1~2.2 天），但中前期的逐点值相差 8~16%。**这不代表谁算错了**，而是两种"收缩运动学"以及"变形坐标系下物性的取值方式"不同所造成的**模型层面**差异。
>
> **为什么差异集中在中前期**：前 12 h 是含水率下降最快、梯度最陡的阶段（干燥锋面刚从表面向内推进），此时"材料点如何随收缩移动"的假设对结果影响最大。到了 36 h 之后剖面已经比较平缓，两种假设给出的差距自然收敛到 1~4%。**记住这个规律——"模型假设的差异在梯度最陡的阶段被放大"是通用的经验。**
>
> **论文里怎么写才不会被扣分**：不要藏起来，要主动写出来并给出解释。推荐措辞："两套独立实现给出的烘干时间分别为 50.71 h 与 52.43 h，相对差异 3.3%，均支持'约 2.2 天'的结论。差异主要出现在干燥前期，源于收缩运动学的处理方式（均匀收缩假设 vs 变形几何平滑求解）以及变形坐标系下 $\rho c_p$ 的取值方式，属于模型层面的不确定度，不影响'收缩显著缩短烘干时间'这一核心结论。"
>
> **想让差异变小（一个可选的加分工作）**：在 COMSOL 里把变形域的空间位移改成显式解析式（例如把域内位移写成 $X\,(R_t-R_{drug})/R_{drug}$），而不是依赖平滑方程求位移，模型就与路线 B 完全一致，两者差值会降到离散误差量级。这是很值得做的一步"对照实验"，可以作为论文里"模型一致性验证"的一小节。

### 5.6 在 COMSOL 里对应什么操作

| 数学概念 | COMSOL 节点 |
|---|---|
| 指标收缩（材料坐标） | 组件 1 → **变形几何** → **变形域**（域 1） |
| 表面随 $R(t)$ 收缩 | **变形几何 → 指定变形**（边界 2），$r$ 方向填 `R_t-R_drug` |
| $r=0$ 固定 | **变形几何 → 指定变形**（边界 1），填 $0$ |
| 表面通量按当前半径算 | 系数型偏微分方程 → 通量/源 1，$g=R_t h_m\left(C_{air}-C\right)$ |

> 因为"变形几何"控制的是**材料坐标系**（对应本文 $g:X_g\mapsto X$ 的映射），所以这条路线与 5.3 节推导的材料坐标模型是同一个模型——这也是你的仿真结果与本文独立解只差 3% 的原因。

> **这张对应表怎么用**：左边是你数学推导里的东西，右边是 COMSOL 界面里点出来的东西，两者描述的是**同一个模型的两种语言**。**论文里只写左边的数学，答辩和复现时才用右边。** 这样你的论文是"数学建模论文"，而不是"软件操作说明书"——这一点在评分上差别很大。
>
> **为什么"变形几何"等价于 5.3 节的材料坐标模型**：因为"变形几何"内部维护的正是"从参考构型到当前构型的映射"$g:X_g\mapsto X$，它在数值上做的事与式 (13) 的映射完全相同（一维情形下平滑方程给出的位移场恰好是线性的）。**这就是为什么你的 COMSOL 结果与路线 B 的独立解只差 3%。**

---

## 第 6 章 数值算法详解（每个算法：是什么 / 为什么 / 怎么做）

### 6.1 有限体积法（FVM）

**是什么**：把计算区间 $[0,R]$ 划分成 $N$ 个控制容积（cell），第 $i$ 个体积为 $V_i$，对守恒方程做**逐格积分**而不是逐点逼近。它天然保证离散后的质量/能量守恒。

**网格**（单元界面 $r_{i\pm1/2}$、节点中心 $r_i$）：

$$r_{i+1/2}=i\Delta r,\quad \Delta r=R/N,\quad
V_i=\pi\left(r_{i+1/2}^2-r_{i-1/2}^2\right),\quad
A_{i+1/2}=2\pi r_{i+1/2}$$

**离散**：对第 $i$ 格积分式 (1) 得

$$\frac{V_i}{\Delta t}\left(C_i^{n+1}-C_i^{n}\right)
=A_{i+\frac12}D_{i+\frac12}\frac{C_{i+1}^{n+1}-C_i^{n+1}}{\Delta r}
-A_{i-\frac12}D_{i-\frac12}\frac{C_i^{n+1}-C_{i-1}^{n+1}}{\Delta r}\tag{19}$$

其中界面扩散系数用相邻两格算术平均 $D_{i+1/2}=\frac12\left(D_i+D_{i+1}\right)$。

**边界处理**：

- 轴心 $r_{1/2}=0\Rightarrow A_{1/2}=0$，对称条件自动满足（不需要另写方程）；
- 表面：把界面通量直接替换为对流通量 $A_R h_m\left(C_{air}-C_N\right)$，即
  $$A_R h_m\left(C_{air}-C_N^{n+1}\right)
  =2\pi R_0 h_m\left(C_{air}-C_N^{n+1}\right)$$
- 温度方程完全类似，把 $D$ 换成 $k/\left(\rho c_p\right)$、$h_m$ 换成 $h$，右端再加 $A_R h\,T_{air}$。

> **"守恒型离散"到底好在哪**：把方程在第 $i$ 个控制体上积分，得到的是"这一格的水分变化 = 从左面进来的 − 从右面出去的"。注意：**每个内部界面上的通量会在相邻两格里各出现一次**（对一格是流出、对另一格是流入），求和时自动抵消。于是全局的总水量变化就等于最外层边界的通量——**离散解天然满足守恒律，不需要任何额外修正。** 这就是质量/能量守恒问题首选有限体积法的原因，也是 8.4 节能用积分恒等式检验数据的底气。
>
> **圆柱坐标下控制体"容量"为什么要用 $\pi(r_{i+1/2}^2-r_{i-1/2}^2)$ 而不是 $\Delta r$**：因为容量是**体积**而不是长度。取差平方可以保证"所有控制体体积之和 $=$ 圆柱总体积"（对 $r$ 是精确积分）；若偷懒用 $2\pi r_i\Delta r$ 近似，就会出现 $\sum V_i\ne V_{total}$ 的微小泄漏，长时间积分会累积成可见误差。论文里可以注明"控制体体积按 $V_i=\pi(r_{i+1/2}^2-r_{i-1/2}^2)$ 精确计算"。
>
> **界面扩散系数 $D_{i+1/2}$ 为什么用算术平均**：$D$ 依赖 $C$，而 $C$ 定义在格子中心、通量定义在格子上——用相邻两格的平均值是一种二阶精度的近似。如果物性在空间上变化极剧烈，更严谨的做法是用**调和平均**（它等价于把两侧材料看成串联的热阻/质阻）。本问题里 $D$ 在空间上的变化是平缓的（单一种材料，只是随 $C$ 渐变），算术平均完全够用，论文里说明一句即可。
>
> **边界为什么"不用特殊处理"**：注意 $A_{1/2}=2\pi r_{1/2}=2\pi\times0=0$，所以轴心那一项在离散方程里自动消失——**对称条件是"免费"得到的**，不需要额外写一个方程。这个"零面积 ⇒ 零通量"的洞察既能让程序少写一个特判分支，也能在论文里体现你对离散格式的理解。

### 6.2 全隐式时间离散与稳定性

**为什么不用显式**：显式格式的稳定性条件是

$$\Delta t\le\frac{\left(\rho c_p\right)\Delta r^{2}}{2k}$$

取 $\Delta r=0.02/400=5\times10^{-5}$ m、$k\approx0.3$、$\rho c_p\approx2\times10^{6}$ 量级，得 $\Delta t\lesssim10^{-3}$ s——算 72 h 需要 $2.6\times10^{8}$ 步，不可行；水分方程同理（$D$ 很小时反而宽松，但两者要取同一个 $\Delta t$）。

**本文做法**：所有空间导数取第 $n+1$ 时刻（**全隐式**，式 (19)），无条件稳定，且 $\Delta t$ 可以取到 10~60 s。数值实验证实：$\Delta t=20/10/5$ s 的烘干时间分别为 56.856/56.856/56.854 h，差异 $<0.004\%$。

> **显式格式为什么会"炸"**：显式格式用旧时刻的值直接算出新时刻，$\Delta t$ 太大时"这一步算出的变化量"会超过附近的值本身，误差就会像滚雪球一样指数放大。对扩散方程，只要 $\Delta t>\dfrac{\Delta r^{2}}{2a}$（$a$ 为扩散率）就会不稳定，这个条件来自 **von Neumann 稳定性分析**。**注意它是平方关系**：$\Delta r$ 减半，$\Delta t$ 要缩到四分之一。这就是为什么加密网格会让显式格式的计算成本爆炸。
>
> **全隐式是怎么"躲开"这个限制的**：隐式格式把空间导数取在新时刻 $n+1$ 上，于是每一步都要解一个线性方程组。代价是每步多解一次方程（$O(N)$ 的追赶法，非常便宜），收益是**无条件稳定**——不论 $\Delta t$ 取多大都不会发散。对本题这种"长时间、慢过程"的问题，全隐式几乎是最自然的选择。
>
> **"稳定性"和"精度"是两件不同的事，千万不要混为一谈**：无条件稳定只说明"不会发散"，**不说明"算得够准"**。$\Delta t$ 太大仍然会有精度问题（时间离散误差是 $O(\Delta t)$）。所以本节末尾仍然要检验"$\Delta t=20/10/5$ s 结果几乎相同"——那是在检验**精度已经足够**，而不是在检验稳定性。**论文里把这两句话分开写，立刻就显得专业。**
>
> **顺手记一个量级感**：本问题若用显式格式需要 $\Delta t\lesssim10^{-3}$ s，算 72 h 要 $2.6\times10^{8}$ 步——这是完全不可行的。把这两个数字写进论文，就能有力地说明"为什么必须用隐式格式"。

### 6.3 三对角方程组的追赶法（Thomas 算法）

**是什么**：式 (19) 对每格整理后得到形如

$$a_i u_{i-1}+b_i u_i+c_i u_{i+1}=d_i\qquad(i=1,\dots,N)\tag{20}$$

的线性方程组，其系数矩阵只有三条对角线（$a_i=0$ 当 $i=1$，$c_i=0$ 当 $i=N$），即**三对角矩阵**。

**为什么用它**：一般高斯消元是 $O(N^3)$，追赶法利用零元素只需 $O(N)$；$N=400$、每步两次求解（温度 + 水分）耗时可以忽略。

**怎么做**（两步扫描）：

1. 前向消元：
   $$c'_1=\frac{c_1}{b_1},\quad d'_1=\frac{d_1}{b_1},\qquad
   c'_i=\frac{c_i}{b_i-a_i c'_{i-1}},\quad
   d'_i=\frac{d_i-a_i d'_{i-1}}{b_i-a_i c'_{i-1}}$$
2. 回代：
   $$u_N=d'_N,\qquad u_i=d'_i-c'_iu_{i+1}\quad(i=N-1,\dots,1)$$

**为什么一定可解**：全隐式格式的系数满足 $b_i>0$、$a_i,c_i<0$ 且 $b_i>|a_i|+|c_i|$（对角占优），故前向消元不会出现零除，数值稳定。

> **"三对角矩阵"为什么这么重要**：矩阵的元素只出现在主对角线和它上下两条副对角线上，其余全是零。存储它只需要 3 个数组而不是 $N^2$ 个，求解它只需要 $O(N)$ 次运算而不是 $O(N^3)$。**判断一个数值问题能不能"算得动"，往往就看它的矩阵长什么样。** 本题是一维、单向耦合，所以矩阵永远是三对角——这是选择一维模型带来的另一个红利。
>
> **追赶法（Thomas 算法）的核心思想**：就是"先从上往下把方程消成只含一个未知量的形式（前向消元），再从下往上逐个代回（回代）"。第一步里出现的除法 $b_i-a_ic'_{i-1}$ 一旦接近零就会数值爆炸，所以需要"分母不为零"的保证——这正是下一段要说的对角占优。
>
> **"对角占优 ⇒ 可解"这条结论值得记住**：全隐式扩散方程离散后天然满足 $b_i>|a_i|+|c_i|$（**严格对角占优**），而这是追赶法数值稳定的充分条件。所以**你不需要担心"万一要除以零"**——只要格式是全隐式的，这件事在数学上就不会发生。论文里可以顺带写一句："系数矩阵严格对角占优，追赶法前向消元不存在零除风险，数值过程稳定。"

### 6.4 非线性物性的处理（滞后系数 / 半隐式）

$D(C,T)$、$\rho(C)c_p(C)$、$k(C)$ 都依赖待求的 $C,T$。做法是**用上一时间步的值计算物性**（滞后一步 / 显式线性化）：

$$D_i^{n+\frac12}=D\left(C_i^{n},T_i^{n}\right),\quad
\left(\rho c_p\right)_i^{n+\frac12}=\left(\rho c_p\right)\left(C_i^n\right)$$

这样每个时间步仍是**线性**三对角方程，误差是 $O(\Delta t)$（与时间离散同阶，不降低整体精度）。若想更精确，可在外层做 2~3 次 Picard 迭代（用最新解重算物性再解一次），本问题不需要。

物理上这样处理也是合理的：物性在 10 s 内变化很小（$C$ 相对变化 $<10^{-4}$）。

> **"滞后系数 / 半隐式"到底在解决什么问题**：物性依赖待求的未知量，方程本来是非线性的，直接求解需要 Newton 迭代。滞后一步的做法是"用**上一时刻**的已知值去计算这一时刻的物性"，于是把一个非线性问题变成了一串线性问题，每步仍然只需解一次三对角方程组。
>
> **这样做合理吗（这是审阅人一定会问的）**：合理，前提是"一个时间步内物性变化很小"。本题 $\Delta t\le60$ s，而含水率的特征变化时间（$\tau_D$）是十几小时，$C$ 在一个时间步内的相对变化只有 $10^{-4}$ 量级，所以滞后引入的误差极小。论文里可以这样写："采用显式线性化（滞后一步）处理变物性；因时间步长足够小（$\Delta t\le60$ s $\ll\tau_D$），引入的误差为 $O(\Delta t)$，与时间离散同阶，不降低整体精度。"
>
> **什么时候必须改成迭代**：如果物性变化剧烈（相变、燃烧、强温度依赖的化学反应），滞后一步会带来明显误差甚至不稳定，此时要在每个时间步内做若干次 **Picard 迭代**（用刚算出的解重算物性，再解一遍，反复几次直到物性几乎不变）或 Newton 迭代。**本题做过数值实验**：加 2~3 次 Picard 迭代对结果没有可见影响，因此不需要。**把"我们试过、发现没必要"这一句写进论文是很加分的——它证明你清楚自己方法的适用边界，而不是碰巧用对了。**

### 6.5 后处理

1. **插值到偶数网格**：COMSOL/有限体积节点不一定正好落在 $0.1$ cm 整数倍处，用线性插值把结果映射到 $r=0,0.1,\dots,2.0$ cm；
2. **四位小数**：按题目要求统一四舍五入到 $10^{-4}$；
3. **时间对齐**：从导出的时刻列表中挑选题目要求的时刻（1 s / 60 s 间隔），避免用序号代替时间。

> **三个后处理步骤，每一步都是在"对题目要求"**：插值是为了让结果落在题目指定的 $0,0.1,\dots,2.0$ cm 网格上；四位小数是题目明文规定的格式；时间对齐是为了满足"每隔 1 s""每隔 60 s"的要求。**这些看起来琐碎的动作，恰恰是提交文件能不能通过格式检查的关键。** 论文里可以放在"结果输出与格式说明"一小节，两三句话即可。
>
> **插值会引入额外误差吗**：会，但极小。线性插值在网格足够密时误差是 $O(\Delta r^2)$，而数值解本身的误差是 $O(\Delta r)$，所以**插值误差是高阶小量**，不会成为主导误差项。论文里一句话带过即可，不必展开。
>
> **一个实操提醒**：如果导出的时间点里没有正好等于 1800 s 或 57 h 的时刻，**不要用"最接近的序号"代替时刻**，而应当按时间做插值。用序号代替时间是这类题目里最常见的隐蔽错误，而且它会让表格里的数字整体偏移，很难被发现。

---

## 第 7 章 COMSOL 有限元模型与本文有限体积模型的对照

### 7.1 两者的数学关系

- 有限体积法：直接对守恒方程在控制容积上积分，**离散严格守恒**；
- 有限元（COMSOL）：把方程写成弱形式，用分片多项式逼近，**整体守恒**（弱形式中边界通量同样进入方程）。

两者离散同一个数学模型，只要网格足够细、时间步相同，结果应趋于同一极限——这正是本文用"两种方法互验"的依据。

> **有限体积法 vs 有限元法，一句话说清区别**：有限体积法直接对"守恒量的收支"做离散，所以离散后**逐格严格守恒**；有限元法把方程乘上试探函数再积分（弱形式），用分片多项式逼近解，所以追求的是**整体上**满足方程。两者都能收敛到同一个真解，只是误差的分布形态和实现难度不同。
>
> **为什么"两种方法对比"本身就是一种有效验证**：因为它们的**误差机理完全不同**——一个是"通量记账的误差"，一个是"函数逼近的误差"。它们可能偶然接近，但很难**系统性地**同时偏离真解同一个量。所以当两种方法在网格、时间步都收敛之后给出相近的答案时，这个答案的可信度远高于单方法的自检。
>
> **论文里怎么用这一段**：不必展开有限元理论，只需说明"本文用有限体积法独立编写程序求解同一模型，作为对有限元（COMSOL）结果的交叉验证"，然后给出对比表。这已经足够构成一个完整的"模型验证"小节。

### 7.2 COMSOL 里圆柱权重 $r$ 的正确处理（容易出错的地方）

COMSOL 的"系数形式偏微分方程"接口在**轴对称几何下不会自动加上圆柱权重 $r$**（官方文档明确说明：轴对称下需要人为乘上体积因子 $r$）。因此你的模型中：

| 设置项 | 正确表达式 | 作用 |
|---|---|---|
| 阻尼或质量系数 `da` | `r` | 提供 $\partial\left(rC\right)/\partial t$ 中的 $r$ |
| 扩散系数 `c` | `r*D_coef` | 提供 $rD\partial C/\partial r$ 的柱面面积因子 |
| 通量/源 1 的边界通量 `g` | `R_drug*h_mass*(C_air-C)`（问题 4：`R_t*...`） | 表面通量 × 当前半径 |
| 源项物理量单位 | `m/s` | 让系数表的期望单位与之匹配 |

如果漏掉 $r$，得到的实际上是**一维平板**模型。本文用数值实验量化了这个错误：同一组参数下，漏掉 $r$ 时烘干时间为 **119.95 h**，加上 $r$ 后为 **57.02 h**——相差一倍，因此这一步必须严格检查。

> **为什么轴对称下必须手动乘 $r$**：柱坐标的守恒方程写成散度形式时，体积微元是 $r\,\mathrm{d}r\,\mathrm{d}\theta\,\mathrm{d}z$，也就是"同样厚度的一层，越往外体积越大"。而 COMSOL 的系数形式 PDE 接口是按**直角坐标的均匀权重**来组装方程与积分的，它并不知道你在用极坐标。因此必须把"$r$ 权重"显式写进各个系数里（质量系数、扩散系数、边界通量），才能还原出真正的轴对称方程。
>
> **漏掉 $r$ 的后果有多严重**：漏掉 $r$ 相当于把圆柱当成**一维平板**。平板没有"越靠外面积越大"的效应，水分逃逸比圆柱慢得多，于是烘干时间从 57 h 变成 120 h（近乎翻倍）。**这个 2 倍量级的差别说明：这类错误不会因为"结果看起来合理"而被发现**，只能靠守恒检验（8.4 节）或与独立解对比（8.2 节）才能揪出来。
>
> **论文里怎么表述最合适**：把它写成"数值实现要点"而不是"操作步骤"——"在轴对称柱坐标下，控制方程需显式引入柱面权重因子 $r$（即 $\mathrm{d}V=2\pi rL\,\mathrm{d}r$）。若忽略该因子，模型将退化为一维平板，预测的烘干时间由 57 h 变为约 120 h。"**用一句话说明"这个细节影响有多大"，比写十行操作步骤更有价值。**

### 7.3 与有限体积结果的差异来源

| 来源 | 量级 |
|---|---|
| 空间离散（有限元二次单元 vs 有限体积一阶步长） | 0.2~0.4%（问题 1、3） |
| 时间步与求解器容差（COMSOL 自适应步长） | <0.1% |
| 问题 4 的收缩运动学与坐标系处理 | ~3% |

> **差异来源必须分类，不能混在一句话里**：这张表的第一行是**离散误差**（网格/时间步造成的，加密网格就会变小），第三行是**模型误差**（建模假设造成的，加密网格再多次也不会变小）。论文的误差分析里把这两类**分列**，能立刻显示出你清楚"哪些误差是我算得不够细、哪些是我假设得不够真"。这比笼统地说一句"误差约 3%"专业得多，也是本手册第 8.6 节的铺垫。

---

## 第 8 章 验证体系：我是怎么验证你的 result1~4.xlsx 的

### 8.1 总框架：四类相互独立的证据

| 证据 | 做法 | 作用 | 判决标准 |
|---|---|---|---|
| ① 独立复算 | 用**另一种数值方法**（有限体积）重写模型并求解，与 COMSOL（有限元）对比 | 排除"程序写错/公式抄错" | 相对差 <1%（问题 1~3）、<5%（问题 4） |
| ② 网格与时间步收敛 | 加密网格（$N=100\to800$）与减小时间步（$\Delta t=20\to5$ s），看结果是否收敛到同一极限 | 排除"离散误差过大" | 网格加密 2 倍后差别减半（一阶收敛），外推极限应接近 COMSOL |
| ③ 全局守恒恒等式 | 用**导出的数据本身**检验积分守恒律 | 排除"边界条件/权重因子写错" | 残差 <1% |
| ④ 定性检验 | 单调性、初值、边界值、极值原理、量纲与量级 | 排除"数值解但不物理" | 全部满足 |

> 关键思想：**不同的证据必须来自不同的角度**。如果只用同一个程序自检，程序里的系统性错误（例如漏掉圆柱权重 $r$）会被完整地复制一遍，检验不出来。证据 ① 用不同方法、证据 ③ 用不依赖任何程序的积分恒等式，才具有独立意义。

> **为什么是"四类"证据，而不是"一类做四遍"**：验证的本质是**证伪**——你要尽量给模型制造"一出错就露馅"的机会。四类证据恰好能捕捉不同种类的错误：
>
> | 证据 | 它专门抓什么错 |
> |---|---|
> | ① 独立复算 | 模型 / 公式抄错（两套方法的公式来源完全不同） |
> | ② 网格与时间步收敛 | 离散误差过大（网格不够密时，加密后结果会明显漂移） |
> | ③ 全局守恒恒等式 | 权因子 / 边界条件写错（漏掉 $r$ 或误用 $R_0$，残差会从 0.5% 跳到几十个百分点） |
> | ④ 定性检验 | "算出来了但不物理"（含水率升高、温度超过烘房温度等） |
>
> **论文里最推荐的呈现方式**：把四类证据合成一张"验证汇总表"（证据 + 判据 + 是否通过），正文只放这一张表，细节放附录。这样既显得严谨，又不占用正文篇幅。**评委读论文的时间很有限，一张表胜过四段文字。**

### 8.2 证据 ①：独立复算（有限体积 vs 有限元）

| 检验量 | COMSOL（你的数据） | 独立有限体积解 | 相对差 |
|---|---|---|---|
| 问题 1：1800 s 中心温度 | 33.5263 ℃ | 33.5811 ℃ | 0.16% |
| 问题 1：1800 s 表面温度 | 36.7598 ℃ | 36.7842 ℃ | 0.07% |
| 问题 1：1800 s 表面含水率 | 1.5088 | 1.5145 | 0.38% |
| 问题 3：烘干时间 | 57.15 h | 57.02 h | 0.23% |
| 问题 4：烘干时间 | 52.43 h | 50.71 h | 3.3% |
| 问题 4：72 h 表面含水率 | 0.0512 | 0.0511 | 0.2% |

另外，问题 2 与问题 3 使用同一套方程和物性，我额外检验了两者在相同时刻的一致性：$t=1$ h 中心含水率 2.5250（问题 2）vs 2.5232（问题 3），$t=3$ h 为 1.7705 vs 1.7757，差 $<0.3\%$——属于两个求解器不同时间步造成的正常差异，说明两个算例的模型是一致的。

> **"相对误差"怎么算、怎么判读**：本文统一用 $\varepsilon=\dfrac{|x_1-x_2|}{|x_1|}\times100\%$，即以独立解（或更细网格的解）为分母。论文里**必须指明分母是谁**，否则读者无法判断"0.2%"是相对于谁说的。一般约定：以更接近真解的那一个（网格更细、或方法更可靠的那个）为基准。
>
> **多大的差异算"通过"**：本手册给的阈值是 1%（问题 1~3）与 5%（问题 4）。这两个数不是随便定的——1% 对应"一阶格式在 $N=400$ 时的离散误差量级"（见 8.3 的收敛表）；问题 4 因为存在模型层面的收缩运动学差异，放宽容限到 5%。**论文里必须给出阈值的来源，否则"通过"就只是一句自我宣布。**
>
> **"问题 2 与问题 3 的一致性检验"为什么值得做**：这两个问题用同一套方程、同一套物性，只是要求的输出不同。把它们在相同时刻、相同位置的结果对比，差异很小（$<0.3\%$）就说明"两个算例其实是一个模型"。这个检验的花费几乎为零，却能有效防止"第二次建模时有参数没改回来"这类低级错误。

### 8.3 证据 ②：网格与时间步收敛性

**问题 3 的烘干时间（h）**

| 网格 | $\Delta t=20$ s | $\Delta t=10$ s | $\Delta t=5$ s |
|---|---|---|---|
| $N=100$ | 56.511 | 56.508 | 56.507 |
| $N=200$ | 56.856 | 56.856 | 56.854 |
| $N=400$ | 57.022 | 57.019 | 57.019 |

**解读**：

1. 时间步的影响 $<0.005\%$（全隐式格式，见 6.2）→ 时间离散已足够细；
2. 网格加密 2 倍，误差（与极限值的差）减半：$N=100\to200$ 变化 0.348 h、$200\to400$ 变化 0.163 h，比值 2.1 ≈ 2，说明空间离散是**一阶收敛**；
3. 用 Richardson 外推（$t_{dry}=t_\infty-K\Delta r$）得极限值
   $$t_\infty\approx57.019+\frac{57.019-56.856}{1}\approx57.19\ \mathrm{h}$$
   与 COMSOL 的 **57.15 h** 相差 0.07%。→ **说明 0.2% 的差异主要来自我这一侧的网格误差，COMSOL 的解更接近真解。**

**问题 1（1800 s 表面含水率）**

| $N$ | 100 | 200 | 400 | 800 | 外推极限 |
|---|---|---|---|---|---|
| 表面含水率 | 1.5268 | 1.5186 | 1.5145 | 1.5124 | ≈1.5103 |

COMSOL 值 1.5088 与外推极限差 0.1%，同样说明差异来自离散。

**问题 4（烘干时间）**

| $N$ | 100 | 200 | 400 | 外推极限 |
|---|---|---|---|---|
| 烘干时间/h | 50.333 | 50.589 | 50.711 | ≈50.83 |

外推后与 COMSOL 的 52.43 h 仍差 3.2%，说明这一差异**不是离散误差**，而是模型细节差异（收缩运动学、变形坐标系下 $\rho c_p$ 的处理），需在论文误差分析中说明。

> **"一阶收敛"是什么意思**：把误差写成 $e\sim C\,\Delta r^{p}$，其中 $p$ 就是**收敛阶**。$p=1$ 表示网格尺寸减半、误差也减半；$p=2$ 表示误差降到四分之一。有限体积法在均匀网格上对扩散方程通常是二阶的，但在"变系数 + 边界 + 时间推进"的组合下常表现为一阶。**知道 $p$（哪怕是数值估计出来的）就能做 Richardson 外推，把离散误差"外推掉"。**
>
> **Richardson 外推的操作步骤**：(1) 用两套网格算出 $t_1$（粗网格 $N=200$）与 $t_2$（细网格 $N=400$）；(2) 确认网格比 $r=\Delta r_1/\Delta r_2=2$，且由上面的表确认 $p=1$；(3) 代入 $t_\infty=\dfrac{r^{p}t_2-t_1}{r^{p}-1}=2t_2-t_1$。本题 $t_2=57.019$、$t_1=56.856$，得 $t_\infty\approx57.18$ h；(4) 再用第三套网格（$N=800$）验证 $t_\infty$ 是否稳定不变——稳定就说明离散误差基本被清掉了。
>
> **这一步在论文里的意义（非常重要）**：它把"我的结果和 COMSOL 差 0.2%"这句话，从"两者不一致"**翻转**成"这 0.2% 是我自己网格造成的；外推后差异只剩 0.07%，说明 COMSOL 的解更接近真解"。**同一组数据，结论完全不同——这就是做误差分析的价值所在。**
>
> **一个实用建议**：做一张"网格 × 时间步"的表（就像本节这样 3×3 的表），比画任何图都有说服力，而且表格方便审阅人逐一核对。**注意表中要同时改变网格和时间步，才能说明两者都收敛**；只变其中一个，只能说"另一个的影响被掩埋了"。

### 8.4 证据 ③：全局守恒恒等式检验（只用导出数据）

把水分方程 (1) 在 $[0,R]$ 上乘以 $r$ 后积分：

$$\int_0^R\frac{\partial C}{\partial t}r\,\mathrm{d}r
=\int_0^R\frac{\partial}{\partial r}\left(rD\frac{\partial C}{\partial r}\right)\mathrm{d}r
=\left[rD\frac{\partial C}{\partial r}\right]_0^R
=-R\,h_m\left(C(R,t)-C_{air}(t)\right)$$

即得到一个与具体数值方法无关的**积分守恒律**：

$$\boxed{\;\frac{\mathrm{d}}{\mathrm{d}t}\int_0^R C(r,t)\,r\,\mathrm{d}r
=-R\,h_m\left(C(R,t)-C_{air}(t)\right)\;}\tag{21}$$

**检验算法（4 步）**：

1. 从 `comsol_p1_C.csv / comsol_p3_C.csv` 读入 $C(r_j,t_k)$ 与节点半径 $r_j$；
2. 用柱面面积权重做数值积分
   $$I(t_k)=\sum_j C(r_j,t_k)\cdot\frac{1}{2}\left(r_{j+1/2}^2-r_{j-1/2}^2\right),\qquad
   r_{j\pm1/2}=\frac{r_j+r_{j\pm1}}{2}$$
   （$r_{j\pm1/2}$ 用相邻节点中点，保证与导出网格一致）；
3. 用中心差分求左端：$\dfrac{\mathrm{d}I}{\mathrm{d}t}\approx\dfrac{I(t_k+\Delta t)-I(t_k-\Delta t)}{2\Delta t}$（$\Delta t=60$ s）；
4. 右端用**同一时刻的表面值** $C(R,t_k)$ 与插值得到的 $C_{air}(t_k)$ 计算，比较两者。

**检验结果**：

| 算例 | 时刻 | 数据算出的 $\mathrm{d}I/\mathrm{d}t$ | 边界通量 $-Rh_m(C_s-C_{air})$ | 相对残差 |
|---|---|---|---|---|
| 问题 1 | 600 s | $-2.962951\times10^{-8}$ | $-2.958926\times10^{-8}$ | $-0.14\%$ |
| 问题 3 | 600 s | $-3.103210\times10^{-8}$ | $-3.090506\times10^{-8}$ | $-0.41\%$ |
| 问题 3 | 3600 s | $-2.290579\times10^{-8}$ | $-2.299626\times10^{-8}$ | $+0.39\%$ |
| 问题 3 | 10800 s | $-1.533872\times10^{-8}$ | $-1.541513\times10^{-8}$ | $+0.50\%$ |
| 问题 3 | 36000 s | $-3.002079\times10^{-9}$ | $-2.996493\times10^{-9}$ | $-0.19\%$ |
| 问题 3 | 72000 s | $-4.159950\times10^{-10}$ | $-4.148779\times10^{-10}$ | $-0.27\%$ |

残差全部小于 0.5%，且**没有系统性偏向**（正负交替），说明主要来源是 60 s 差分与网格插值误差，而不是模型误差。这条检验同时确认了：圆柱权重 $r$、边界通量中的半径因子（问题 4 用 $R_t$）都是对的——**如果漏掉 $r$ 或把 $R_t$ 写成 $R_0$，残差会立刻变成几十个百分点。**

> **式 (21) 是怎么推出来的（三步，建议在论文里写全）**：(1) 把水分方程 (1) 两边同乘 $r$；(2) 对 $r$ 从 $0$ 积到 $R$；(3) 右端变成 $\int_0^R\dfrac{\partial}{\partial r}\left(rD\dfrac{\partial C}{\partial r}\right)\mathrm{d}r=\left[rD\dfrac{\partial C}{\partial r}\right]_0^R$，这是一个**全微分的积分，只需边界值**——$r=0$ 那一项因为 $r=0$ 而自动为零，$r=R$ 那一项代入边界条件 (4) 就得到 $-Rh_m\left(C(R,t)-C_{air}(t)\right)$。
>
> **为什么说它"不依赖任何数值方法"**：(21) 是连续方程的**严格推论**，与你怎么离散、用什么软件完全无关。因此它是一条**客观标准**：任何一组自称是本题解的数据，都必须满足它。这也正是它能抓出"漏掉 $r$"这类错误的根本原因——漏掉 $r$ 的模型，其守恒律形式都不一样，残差会立刻变成几十个百分点。
>
> **残差 0.5% 的来源要能解释**：(1) 时间导数用中心差分近似，误差 $O(\Delta t^2)$；(2) 积分用节点中点构造，与导出网格存在微小不一致；(3) 表面值 $C(R,t)$ 本身带有离散误差。**关键不是"残差有多小"，而是"残差有没有系统性偏向"**：本文六个时刻的残差正负交替（−0.14%、−0.41%、+0.39%、+0.50%、−0.19%、−0.27%），说明它们来自随机性质的离散误差，而不是某处写错。**如果残差全是同一个符号、并随时刻单调变化，那就是模型存在系统偏差的信号——这一点比残差的绝对大小更重要。**
>
> **论文里可以直接用的表述**："以式 (21) 的积分守恒律对导出数据做检验，6 个抽样时刻的相对残差均小于 0.5%，且正负交替、无系统性偏差，表明数值解满足全局质量守恒，柱面权重因子与边界通量的实现正确。"

### 8.5 证据 ④：定性检验清单

| 检验项 | 期望 | 结果 |
|---|---|---|
| 初始值 | $t=1$ s（问题 1、2）/ $t=60$ s（问题 3、4）时接近 $T_0=28$ ℃、$C_0=2.55$ | ✓ |
| 温度单调性 | 每个位置温度随时间单调不减（烘房比药材热） | ✓ 全部满足 |
| 含水率单调性 | 每个位置含水率随时间单调不增 | ✓ 全部满足 |
| 径向剖面方向 | 温度"外高内低"、含水率"内高外低" | ✓ 全程成立 |
| 极值原理 | 含水率不低于 $C_{air}$、温度不高于 $T_{air}$ | ✓ |
| 中心滞后 | 含水率下降只从表层开始（扩散长度 $\sqrt{Dt}$ 估计） | ✓ 与 $\ell$ 估计一致 |
| 数量级 | 烘干时间应与 $\tau_D$ 同量级（19.7 h → 57 h，考虑 $Bi_m$ 与 $D(C)$ 衰减合理） | ✓ |
| 题目背景 | 问题 2、3 应为"2~3 天" | ✓ 57.15 h = 2.38 天 |
| 问题 4 与问题 3 的关系 | 收缩使干燥加快（$D$ 更小但路径更短），结果应更小 | ✓ 52.43 h < 57.15 h |

> **极值原理在这里的具体用法**：它给出两个"不该被越过"的界限——含水率不该低于 $C_{air}$（空气是唯一的源/汇，表面只能趋近它而不能越过它），温度不该高于 $T_{air}$（同理）。**一旦计算结果越过了这两条，就说明程序有问题**，不必等结果看起来奇怪再去排查。这类"免费的物理约束"非常值得在编程时写成断言（assert）自动检查。
>
> **"中心滞后"这条怎么当检验用**：$\ell\sim\sqrt{Dt}$ 给出"变化只发生在表面附近厚度约 $\ell$ 的一层"。$t=1800$ s 时 $\ell\approx0.3$ cm，对应"只有最外两三列在变、中心仍是 2.5500"。**这两者是互相独立的估计与结果，二者吻合，说明模型行为符合扩散的定性规律**——一个来自解析估算，一个来自数值解。
>
> **"量级检验"是一条很容易被忽略但很有效的证据**：烘干时间应当与扩散特征时间 $\tau_D$ 同量级（19.7 h → 57 h，考虑 $Bi_m\sim3$ 与 $D$ 随 $C$ 衰减后，2~3 倍是合理的）；同时题目背景提示"2-3 天"。**两条独立线索指向同一个量级，就是很强的合理性证据。** 反过来，如果算出来是 5 h 或 500 h，就说明一定有地方错了——**在被别人发现之前，自己先用这个办法筛一遍。**

### 8.6 误差来源与量级汇总（论文误差分析可照抄）

| 来源 | 类型 | 量级 |
|---|---|---|
| 空间离散 | 数值 | 问题 1、3：0.2~0.4%（随网格一阶收敛） |
| 时间离散 | 数值 | $<0.01\%$（全隐式） |
| 非线性物性滞后一步 | 数值 | $O(\Delta t)$，$<0.05\%$ |
| 经验公式本身的适用范围 | 模型 | 题目给定，无法评估，在假设中说明 |
| 一维简化（忽略端面） | 模型 | 端面占面积 8% |
| 收缩运动学假设（同比例收缩） | 模型 | 问题 4：约 3% |
| 插值（烘房条件外推、半径插值） | 模型 | 温和（分段线性），$<1\%$ |

> **"数值误差"和"模型误差"是两类完全不同的东西，这是本节最重要的一句话**：
>
> - **数值误差**来自"把连续问题算成离散问题"——网格、时间步、迭代容差。它的特点是**可以通过加密网格、减小时间步来削减**，而且可以用收敛性研究量化。
> - **模型误差**来自"把真实问题写成数学问题"——一维简化、均匀收缩假设、经验公式的适用范围。它**不会因为算得更细而变小**，只能通过论证假设的合理性、做敏感性分析来控制。
>
> **为什么论文里必须把两类分开写**：审阅人看到"总误差 3%"一定会问"这 3% 是因为你算得不够细，还是因为你的模型不对？"。分开写就能回答：问题 4 的 3.3% 经收敛性检验证明**不是**数值误差，而是模型层面的收缩假设差异——**这句话的含金量远高于一个笼统的数字。**
>
> **还要补一句"误差不影响结论"的论证**：只要误差量级远小于结论所需的区分度，结论就是稳健的。例如"两套实现给出的烘干时间相差 3.3%，但仍共同支持'约 2.1~2.2 天'的结论，且都远小于不考虑收缩时的 72 h 以上，因此'收缩显著缩短烘干时间'这一核心结论不受该不确定度影响"。

### 8.7 论文可直接使用的"模型验证"段落

> 为检验模型与结果的可信度，本文从四个相互独立的角度进行验证：(1) 用有限体积法独立编写程序复算，与 COMSOL 有限元结果对比，问题 1、3 的相对偏差分别为 0.16%~0.38% 与 0.23%，问题 4 为 3.3%；(2) 进行网格与时间步收敛性检验，网格加密一倍误差减半（一阶收敛），Richardson 外推得到的烘干时间为 57.19 h，与 COMSOL 的 57.15 h 相差 0.07%，说明差异主要来自离散误差；(3) 用与数值方法无关的积分守恒律 (21) 检验导出数据，残差全部小于 0.5% 且正负交替，验证了圆柱权重与边界通量的正确性；(4) 定性检验表明解满足单调性、极值原理与量级估计，烘干时间 57.15 h（约 2.4 天）与题目"2~3 天"的先验判断一致。

---

## 第 9 章 论文写作与结果文件对照

### 9.1 各问写作要点

| 问题 | 模型 | 关键公式 | 关键结论 | 结果文件 |
|---|---|---|---|---|
| 1 | 常物性轴对称扩散 | (1)(2)(3)(4)(5)(6)+(8) | 30 min 内只有表层 0.3 cm 失水；表面 1.5088 | result1.xlsx（温度/水分浓度） |
| 2 | 变物性耦合扩散 | 同上 +(10)(11) | 3 h 内温度趋于均匀，含水率呈"外干内湿" | result2.xlsx（温度/水分浓度） |
| 3 | 变物性耦合 + 烘干判据 | (12) 及中心判据 | 烘干时间 57.15 h（2.4 天） | result3.xlsx（Sheet1，水分浓度） |
| 4 | 材料坐标 + 尺寸收缩 | (13)~(17) | 收缩使烘干时间缩短到 52.43 h；不可忽略收缩 | result4.xlsx（Sheet1，末列药材表面） |

> **这张表建议当成"写作检查清单"用**：每写完一问，就回来对一遍——模型写了吗？关键公式编号了吗？结论给数了吗？对应结果文件生成了吗？四项齐全，这一问才算完整。
>
> **一个容易忽略的细节**：表 5、表 6 的最后一行是"烘干结束时间"。这一行应当填**计算得到的烘干时间**（问题 3：约 57.2 h；问题 4：约 52.4 h），并把该时刻各位置的含水率一并列出，这样"最后一行全部低于 0.15"这一事实就能被审阅人**直接看到**，不需要他自己去核对。**表格自己会说话，比正文解释更有力。**

### 9.2 建议的图表清单

1. 图：几何与坐标系示意图（圆柱 + 微元）；
2. 图：问题 1 的温度/含水率径向剖面（$t=100,600,1200,1800$ s）；
3. 图：问题 2/3 的中心与表面含水率随时间的曲线（对数时间轴更清楚）；
4. 图：问题 3 含水率场云图（$r$–$t$）；
5. 图：问题 4 中心含水率随时间（并叠加问题 3 曲线，突出收缩影响）；
6. 图：问题 4 半径 $R(t)$ 与由数据反推的形状变化；
7. 图（可选）：网格收敛曲线（$N$–烘干时间）；
8. 表：验证汇总表（8.2、8.3 节）。

> **图的目的不是好看，而是每张图各回答一个不同的问题**：剖面图回答"某一时刻水分/温度是怎么分布的"；时间曲线回答"某个位置随时间怎么变化"；云图（$r$–$t$）回答"整个过程的快慢结构"；对比图回答"模型 A 与模型 B 差别在哪"。**每张图都要能对应到论文里的一句话结论，做不到这一点的图就删掉。** 图不必多，但每一张都要有明确的"说理任务"。
>
> **对数时间轴什么时候用**：当关注的现象跨越多个数量级时（前期几分钟、后期几十小时），线性时间轴会把前期挤成一条竖线，看不出结构。问题 3、4 的干燥曲线建议**在同一张图里放两张子图**（线性 + 对数），这样前期与后期的快慢都能看清。
>
> **云图的两个实用画法**：把颜色条统一到同一量程，便于不同模型之间对比；在云图上叠加 0.15 的等值线，就能**用几何方式直接读出**烘干时间，比再从数据里查表直观得多。

### 9.3 常用文件与脚本

| 文件 | 说明 |
|---|---|
| `result1~4.xlsx` | 题目要求的四个结果文件 |
| `_tables.py` | 一键抽出论文表 1~表 6 的数值 |
| `ref_q3.py` | 问题 3 的独立有限体积参考解（可验证、也可用于画图） |
| `ref_p4.py` | 问题 4 的三种模型对比（不收缩 / 收缩） |
| `_cross_check.py` | 四问的交叉验证脚本（生成 8.2 节的表格） |
| `_verify_extra.py` | 守恒性恒等式检验 + 网格/时间步收敛性（生成 8.3、8.4 节的表格） |
| `verify_results.py` | 检查 result1~4.xlsx 的结构与数值合理性 |

运行方式（在 `C:\Users\Wil\Documents\ChatGPT\CUMCM` 目录下）：

```
python ref_q3.py
python ref_p4.py
python _cross_check.py
python _verify_extra.py
python _tables.py
python verify_results.py
```

> **运行脚本的顺序有讲究**：先跑 `ref_q3.py`（生成独立参考解），再跑 `_cross_check.py`（对比 COMSOL 与独立解）、`_verify_extra.py`（守恒与收敛检验）——它们会输出 8.2~8.4 节表格里的数字；最后才跑 `_tables.py` 抽取论文表格、`verify_results.py` 检查提交文件的结构。**顺序反过来（先出表后验证）的话，一旦验证发现问题，表格就得重做。**

---

## 附录 A 四问参数速查

| 参数 | 问题 1 | 问题 2、3 | 问题 4 |
|---|---|---|---|
| $\rho$ | 820 | $650+128C$ | $760+90C$ |
| $c_p$ | 2600 | $1450+2736\frac{C}{C+1}$ | $1850+2150\frac{C}{C+1}$ |
| $k$ | 0.36 | $0.21+0.38\frac{C}{C+1}$ | $0.12+0.20\frac{C}{C+1}$ |
| $D$ | $7\times10^{-9}e^{-0.89/C}$ | $2.4\times10^{-3}e^{-0.45/C}e^{-3850/T}$ | $4.2\times10^{-4}e^{-0.30/C}e^{-3850/T}$ |
| 半径 | 0.02 m 固定 | 0.02 m 固定 | $R(t)$：0.02 → 0.01198 m |
| 时间范围 | 1800 s | 10800 s / 约 57 h | 约 52 h |
| 结果 | 1.5088（1800 s 表面含水率） | 57.15 h | 52.43 h |

> **附录 A 是参数的"单一事实来源"**：写论文时如果某个地方给出的物性与这里不一致，那一定是那里写错了，而不是这里错了。建议在论文里也放一张类似的参数表（很多队伍把它放在"符号说明"之后），这样正文里就不必反复重复公式，篇幅也更紧凑。
>
> **最后一次核对清单**：表中 $\rho$、$c_p$、$k$、$D$ 四行是**最容易抄错**的地方——尤其是问题 2/3 与问题 4 的 $D$：前因子（$2.4\times10^{-3}$ vs $4.2\times10^{-4}$）与指数（$e^{-0.45/C}$ vs $e^{-0.30/C}$）**都不同**。抄错会让烘干时间偏出好几小时，而结果看上去仍然"像那么回事"，所以必须在写完后逐项核对一遍。

## 附录 B 常见陷阱速查

1. **圆柱权重 $r$**：COMSOL 轴对称下需手动乘 $r$（`da=r`、`c=r*D`、`g=R*h_m*(C_air-C)`）；漏掉会退化为一维平板，烘干时间从 57 h 变成 120 h。
2. **`degC` 单位带偏移**：初值直接写 `301.15[K]`，不要写 `28[degC]` 再参与运算。
3. **温度经验公式用开尔文**：$e^{-3850/T}$ 中的 $T$ 单位必须是 K。
4. **变量作用域**：变量要建在"组件 1 → 定义 → 变量"下，建在"全局定义"里读不到 $T,C$。
5. **导出数据集**：必须选"研究 1/解 1"；"一维栅格"算不了场变量。
6. **改完模型必须重新计算**，否则导出的是旧解。
7. **问题 4 的边界通量要用当前半径** $R_t$，而不是 $R_0$。
8. **变形几何属于组件下的分支**：右键"组件 1"只能拿到域级节点（变形域等），边界级节点要右键"变形几何"分支。

> **这八条里，第 1、7 条是"会改变结论"的错误，其余是"会浪费你时间"的错误**。第 1 条（漏掉圆柱权重 $r$）会让烘干时间从 57 h 变成 120 h；第 7 条（问题 4 的边界通量误用 $R_0$ 而不是 $R_t$）会让收缩效应失效，结论直接退回问题 3。**这两条在每次建模或改模型之后都要专门检查一遍。**
>
> **一个通用的自检习惯**：模型跑完之后，先去 8.4 节的守恒残差上看一眼。如果残差异常大，几乎总是这几个陷阱之一——**先查权重因子和边界通量里的半径，再查单位**。这个顺序能解决绝大部分"结果不对"的问题。
